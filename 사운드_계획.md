# 사운드 계획 — 2026-08-25

`data/sound.json` 이 큐 목록·재생 규칙의 단일 소스다. 이 문서는 **어떻게 조달하고
어떤 순서로 붙이는지**만 다룬다. 구현은 아직 안 했다 — 후보군을 단장이 직접
들어보고 고른 뒤에 붙인다.

## 방식 — SFX 는 합성, BGM 은 파일

| | SFX | BGM |
|---|---|---|
| 조달 | **ZzFX 런타임 합성** (파라미터 20개짜리 함수 호출) | 외부 생성 (Suno 등) → mp3 |
| 용량 | 0 (코드 ~1KB) | 곡당 약 1MB × 4곡 |
| 조정 | 파라미터 숫자만 바꾸면 즉시 다른 소리 | 재생성 |
| 픽셀 감성 | 레트로 칩튠과 정합 | 곡 스타일로 맞춤 |

왜 파일이 아니라 합성인가 — sound.json 의 SFX 55종을 파일로 사면 라이선스 관리가
따라오고, AI 생성 효과음은 품질 편차가 크다. ZzFX 는 파라미터가 곧 소리라
**후보를 코드로 나열하고 즉석에서 비교 청취**할 수 있다. 마음에 안 들면 숫자
하나 바꾸면 된다.

## 청취 랩 — **만들었다 (2026-08-25)**

`node tools/gen-sfx-lab.mjs` 로 굽고 `tools/sfx-lab.html` 을 브라우저로 연다.
큐 목록의 단일 소스는 `data/sound.json` 이라, 큐가 늘면 다시 구우면 따라온다.
소리는 **단장이 직접 듣고 고른 것만** 게임에 들어간다 — 지금 게임 코드에는
효과음이 한 줄도 안 붙어 있다.
- sound.json 의 큐 55종이 표로 나열 (P0/P1/P2 필터)
- 큐마다 **후보 3개** 버튼 — 누르면 즉시 재생
- 선택한 조합을 JSON 으로 내보내기 → `game/public/data/sfx-params.json` 으로 저장
- 게임 코드는 그 파일을 읽어 재생한다 — **소리 교체에 코드 수정이 없다**

후보는 **음량을 맞춰서 굽는다.** 원형마다 진폭이 달라서 저역 계열(둔탁한 퍽·
낮은 붕·묵직한 쾅)은 피크 0.003, 고역 계열은 0.15 로 50배가 벌어졌다 — 나란히
들으면 조용한 쪽은 성격이 아니라 음량 때문에 탈락한다. 파형을 미리 재서 첫
인자(volume)로 피크를 0.14 근처에 맞췄다(편차 3.1배). 고른 값이 그대로
내보내지므로 게임에서도 같은 크기로 난다.

큐 55종 × 후보 3안 = 165개를 성격이 서로 다르게 잡아 뒀다 (원형 14종 조합).
후보 예 — 실제 표는 랩 페이지에 있다:
- `sfx_hit_melee` — ①둔탁한 퍽 ②금속 챙 ③짧은 톡 (2종 교대는 재생기가 처리)
- `sfx_crit` — ①상승 삑! ②파열음 ③묵직한 쾅 (일반 타격과 확실히 구분)
- `sfx_denied` — ①낮은 붕 ②둔한 똑 ③삐- (P0 — 없으면 버튼이 먹었는지 모른다)
- `sfx_tap` — ①마른 톡 ②물방울 ③클릭
- `sfx_summon_result` — ①팡파레 짧게 ②반짝 상승음 ③뽕망치
- …나머지 21종도 같은 방식. 전체 표는 랩 페이지에 담는다

## 재생기 (붙일 때 한 번만 구현)

`game/src/core/sfx.js` — sound.json > playbackRules 를 그대로 코드로:
- 동시 8개 상한 · 같은 큐 60ms 간격 · 배치 축약(700연차 = 1회 + 최고 등급 팡파레)
- 첫 터치에서 AudioContext 언락 (자동재생 정책)
- 설정 화면의 효과음/배경음 슬라이더(이미 있음)에 연결

## BGM 4곡 — 단장 조달

sound.json > bgm 그대로: 메인(밝은 모험 루프·최우선) / 보스(타악 긴장) /
연합 마을(느긋) / 상점(가벼움). mp3 96kbps 스테레오, 곡당 ~1MB.
`game/public/assets/bgm/` 에 넣으면 재생기가 lazy 로드한다.

## 순서

1. ~~sfx-lab.html + 후보군 작성~~ **완료** (55종×3 = 165개)
2. 단장 청취·선택 → sfx-params.json 확정 ← **여기서 멈춰 있다**
3. 재생기 구현 + 게임 이벤트에 큐 연결 (P0 → P1 → P2)
4. BGM 파일 수급되는 대로 루프 재생 연결
