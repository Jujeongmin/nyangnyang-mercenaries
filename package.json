{
  "name": "nyangnyang-mercenaries",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate": "node tools/validate.mjs",
    "audit": "node tools/audit.mjs",
    "check": "npm run validate && npm run audit && npm run assets",
    "dev:host": "vite --host",
    "assets": "node tools/check-assets.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.21"
  },
  "dependencies": {
    "@verse8/ads": "^0.5.0"
  }
}
