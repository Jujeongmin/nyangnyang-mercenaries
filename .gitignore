# 의존성·캐시
node_modules/
__pycache__/

# Vite 빌드 산출물
dist/
.vite/

# 생성물 — node tools/prompts.js 로 재생성된다
prompts/

# 백업·임시
*.bak
9stest.html
game/public/assets/ui/_*.png

# 옛 생성분 백업 — 저장소에 안 올린다 (로컬 보관)
assets_bak/
assets_raw/
assets_legacy/
.claude/

# 배경 지우개 백업 — 로컬 보관
.bgedit-backup/

# WebP 로 구운 그림의 PNG 원본 — 배포에 안 들어가므로 로컬 보관
# (game/public/ 은 통째로 dist 에 복사된다)
art-src/
