# 냥냥 용병단 — 광고 · 결제 현황

## 광고

* **전면 광고 — 없음.** 게임 어디에도 강제 노출 광고를 넣지 않았다. `game/src/net/ads.js` 는 보상형(rewarded) 한 종류만 내보낸다.
* **자발적 광고(Opt-in) — 유저가 버튼을 눌러야만 재생된다.** 끝까지 봐야 보상이 나가고, 중간에 닫으면(`dismissed`) 횟수도 안 깎이고 보상도 없다.
* **부활 광고 — 없음.** 전투 중 사망 시 부활 UI 자체가 없다(`combat.json`: "즉시 전투에서 제외. 부활 없음").
* 광고망을 직접 부르지 않는다. `postMessage` 로 Verse8 호스트에 넘기는 구조라 **verse8.io 웹셸/모바일 WebView 안에서만** 동작한다.

### 실제로 붙어 있는 지면 (4개)

| 지면 id | 자리 | 보상 | 일일 상한 |
|---|---|---|---|
| `idle_double` | 방치 보상 수령 | 직전 방치 보상 ×2 | 2회 |
| `instant_claim` | 방치 2시간분 즉시 수령 | 2시간분 즉시 지급 | 2회 (무료 2회를 다 쓴 뒤에만 열림) |
| `arena_entries` | 아레나 입장권 창 | 도전 횟수 +3 | 1회 |
| `dungeon_keys` | 던전 열쇠가 0일 때 그 줄의 칩 | 열쇠 +1 | 던전마다 2회 |

### 기획엔 있으나 아직 미구현 (4개)

`dailies.json > adRewards.slots` 에 정의만 있고 코드 연결 없음.

* `ad_free_summon` — 무료 소환 1회 (일 3회)
* `ad_dungeon_entry` — 전 던전 입장 +2 (일 1회)
* `ad_forge_speedup` — 제련 시간 −30분 (일 5회)
* `ad_shop_refresh` — 아레나 매칭 목록 갱신 (일 3회)

기획상 총 일 15회가 상한, 실제 소화율 30~50% 예상.

### 광고와 과금의 관계

* **프리미엄 패스는 광고를 없애지 않는다.** 광고 횟수 상한만 +5 늘린다 — 광고 수익을 유지하면서 편의를 판다.
* **프리미엄(`premium_pack`)만 광고를 제거한다.** 단 버튼은 화면에 그대로 남고, 누르면 광고 재생 없이 즉시 보상이 나간다. 일일 횟수 상한은 동일하다.

---

## VX 결제

100 VX = $1. **가격의 진짜 값은 VX 대시보드**이고, 데이터의 `vx` 는 목록이 도착하기 전이나 호스트 밖에서만 쓰는 폴백이다.

### 다이아 묶음 (`economy.json`)

| 상품 id | 다이아 | VX |
|---|---|---|
| `pack_xs` | 1,500 | 100 |
| `pack_s` | 8,000 | 500 |
| `pack_m` | 17,000 | 1,000 |
| `pack_l` | 55,000 | 3,000 |
| `pack_xl` | 100,000 | 5,000 |
| `pack_xxl` | 220,000 | 10,000 |

### 패키지 (`shop.json > packages`)

| 상품 id | 이름 | 유형 | VX | 내용 |
|---|---|---|---|---|
| `starter_pack` | 신규 성장 패키지 | 계정 생성 후 7일 한정 | 500 | 다이아 4,800 · 용병권 30 · 스킬권 20 · 장비권 100 |
| `premium_pack` | 프리미엄 | 1회 구매 | 1,000 | 3배속 영구 해금 + **광고 제거** + 매일 다이아 100 우편 배달 |
| `growth_pack_50` | 성장 지원 I | 1회 구매 | 500 | 다이아 3,000 · 장비권 200 |
| `growth_pack_100` | 성장 지원 II | 1회 구매 | 1,000 | 다이아 6,000 · 5분 가속 50 · 장비권 500 |
| `growth_pack_150` | 성장 지원 III | 1회 구매 | 2,000 | 다이아 12,000 · 5분 가속 100 · 장비권 1,200 |

### 구독 — 프리미엄 패스 (`pass_premium_s{seasonId}`)

월 구독. **시즌마다 SKU 가 새로 생긴다.**

* 매일 다이아 500 (월 15,000)
* 제작 슬롯 +1 (총 2)
* 제작 타이머 −20%
* 광고 횟수 상한 +5
* 던전 입장 +2 · 아레나 입장 +3

### 다이아로 사는 것 (결제 아님)

* 아레나 입장권 — 다이아 100 / 1장, **상한 없음**

---

## 광고 제거 상품

별도 상품이 없다. `premium_pack` 하나에 **3배속 영구 해금 + 광고 제거**를 묶었다 (구 `speed3_unlock` 과 `adfree_pack` 을 합친 것).

---

## 지금 상태에서의 주의점

1. **지급 주체가 아직 클라이언트다.** Verse8 공식 문서는 서버(`$onItemPurchased`)가 지급해야 한다고 못 박지만, 현재는 세이브 자체가 클라 판정이라 `VXShop.onClose` 콜백에서 지급한다. 서버 권한으로 옮기는 것이 다음 단계다.
2. **광고 보상도 서버 검증이 아직 없다.** `dailies.json` 에 "Verse8 Ads 서버 검증 필수. 클라 콜백만 믿으면 조작된다" 로 적혀 있고, 검증 API 는 https://docs.verse8.io/ko/docs/ads/server-verification 다.
3. **호스트 밖에서는 광고·결제가 모두 죽는다.** 로컬 dev 나 직접 배포한 페이지는 top-frame 이라 `unsupported_env` 로 끝난다. 개발 빌드에서만 광고가 통과되도록 열어 뒀고, 이 분기는 프로덕션 번들에서 통째로 지워진다.
