# 연출 기획 — 던전 · 아레나

작성 2026-08-24. **아직 구현 전이다.** 무엇을 왜 만들지만 적는다.

---

## 0. 지금 어떻게 생겼나

두 콘텐츠 다 **전투 화면을 안 쓴다**. 눌렀을 때 일어나는 일 전부:

| | 지금 |
|---|---|
| 던전 | 열쇠 1개 차감 → 전투력 비교 → 토스트 한 줄 (`황금 광산 3층 돌파 — 273k`) |
| 아레나 | 입장 1회 차감 → 승률로 주사위 → 화면 가운데 글자 (`승리! +30`) |

**전투가 없다.** 승패는 이미 정해진 숫자 비교이고, 화면은 결과만 알린다.
그래서 유저 입장에서는 "버튼을 눌렀더니 숫자가 늘었다" 가 전부다 —
용병을 모으고 편성한 것이 **눈에 보이는 자리가 없다**.

무한의 탑은 다르다. `runTowerFloor` 가 실제 전투 씬을 돌리고, 보스전은
VS 컷(`#vs`)까지 띄운다. **탑에 있는 것을 던전·아레나에도 준다**는 것이
이 기획의 뼈대다.

---

## 1. 원칙 세 가지

**판정은 그대로 둔다.** 던전은 전투력 비교, 아레나는 승률 주사위 — 서버로
옮길 때 그대로 가져갈 수 있어야 한다. 연출은 **이미 정해진 결과를 보여 주는
껍데기**다. 연출이 판정을 바꾸면 서버 검증과 어긋난다.

**기다리게 하지 않는다.** 방치형이다. 던전은 하루 3번, 아레나는 5번 누른다.
한 번에 3초를 넘기면 다섯 번째에는 스킵을 찾는다. **전 구간 2.5초 안**,
그리고 **스킵 가능**.

**이미 있는 것을 쓴다.** 전투 씬(`BattleScene`), VS 컷(`#vs`), 결과 배너
(`showResult`), 등급 오라, 스킬 이펙트가 다 있다. 새 시스템이 아니라
**배선**이다.

---

## 2. 던전 연출

### 2-1. 흐름

```
[입장 버튼]
  ↓ 0.0s  화면 전환 — 던전 배경으로 페이드 (DG-0N, 0.25s)
  ↓ 0.3s  층 간판 — "황금 광산 3층" 이 위에서 떨어져 박힌다
  ↓ 0.6s  파티 등장 — 왼쪽에서 5명이 순차로 슬라이드 인 (60ms 간격)
  ↓ 1.0s  수문장 등장 — 오른쪽에서 등장 + 화면 살짝 흔들림
  ↓ 1.2s  ── 여기서 판정 결과에 따라 갈린다 ──
```

**성공** — 파티가 일제히 돌진, 수문장이 한 방에 무너진다(HIT-08 + 파티클).
층 간판이 `3층 → 4층` 으로 뒤집히고, 보상 재화가 아이콘째 화면 가운데에서
튀어나와 상단 재화 칩으로 빨려 들어간다. (총 1.3s)

**실패** — 파티가 돌진하다 튕겨 나온다(뒤로 밀리는 트랜스폼 + 붉은 화면 플래시).
수문장은 그대로 서 있고, 부족한 전투력이 숫자로 뜬다:
`전투력 720k 필요 · 지금 540k`. (총 1.0s)

### 2-2. 왜 이렇게 하나

- **층 간판이 뒤집히는 것**이 핵심 보상이다. 골드 액수보다 "한 층 더 올라갔다"
  가 진행 실감을 만든다. 숫자는 그 뒤에 따라온다.
- **실패도 연출한다.** 지금은 실패해도 토스트 한 줄이라 열쇠를 헛되이 쓴 느낌만
  남는다. 튕겨 나오는 그림이 있으면 "더 키워서 와야겠다" 로 읽힌다.
- 수문장은 **던전마다 다른 보스 스프라이트**(B-01~B-06)를 쓴다. 던전 다섯 개가
  같은 적을 쓰면 어디에 있는지 헷갈린다.

### 2-3. 필요한 것

| | |
|---|---|
| 코드 | `runDungeon` 을 async 로 바꾸고 전투 씬을 태운다. 탑의 `runTowerFloor` 와 같은 구조 |
| 씬 | `scene.setBackground(DG-0N)` · `setParty` · 수문장 1마리 스폰 · 승패 연출 두 갈래 |
| 신규 에셋 | **없음.** 던전 배경(DG-01~06)·보스(B-01~06)·타격 이펙트 전부 있다 |
| 새 UI | 층 간판(`#dgSign`) — 위에서 떨어져 박히고 숫자가 뒤집히는 것 하나뿐 |

### 2-4. 스킵

두 번째 입장부터 **결과만 보기** 토글이 상단에 뜬다. 켜면 지금처럼 토스트 한 줄.
설정에 저장한다 — 매번 끄고 켜게 하면 그 자체가 피로다.

---

## 3. 아레나 연출

### 3-1. 흐름

```
[도전 버튼]
  ↓ 0.0s  VS 컷 — 내 단장(왼쪽 아래) vs 상대 단장(오른쪽 위)
          이미 있는 #vs 를 그대로 쓴다. 가운데 "VS" 가 번쩍
  ↓ 0.9s  콜로세움 배경으로 전환 (BG-03)
  ↓ 1.1s  양쪽 파티 5명씩 마주 보고 정렬 — 상대 편성은 실제 데이터
  ↓ 1.4s  일제 돌진 · 이펙트 난타 (0.8s, 스킬 이펙트 2~3개 랜덤)
  ↓ 2.2s  ── 결과 ──
```

**승리** — 상대 파티가 뒤로 날아가 사라지고, 점수 칩이 `1061 → 1091` 로
카운트업하며 금빛으로 번쩍인다.

**패배** — 내 파티가 무릎을 꿇고 화면이 회색으로 빠진다. 점수가 내려가는 것을
**숫자가 깎이는 애니메이션**으로 보여 준다. 감추지 않는다 — 진 것을 흐리면
다음에 왜 이겨야 하는지가 안 남는다.

### 3-2. 왜 이렇게 하나

- 아레나의 값은 **남의 편성을 본다**는 데 있다. 상대 파티 5명이 실제로 화면에
  서야 "저 조합에 졌구나" 가 남는다. 지금은 상세 창을 따로 눌러야 보인다.
- VS 컷은 **이미 만들어 놓고 보스전에만 쓰고 있다**. 아레나야말로 VS 가 맞는
  자리다 — 사람 대 사람이라는 것이 한 장으로 읽힌다.
- 난타 구간을 0.8초로 짧게 두는 이유: 승패가 이미 정해져 있으므로 길어질수록
  "결과를 알면서 기다리는" 시간이 된다.

### 3-3. 필요한 것

| | |
|---|---|
| 코드 | `arenaFight` 를 async 로. VS 컷 → 씬 전환 → 결과. 판정은 지금 그대로 앞에서 계산해 두고 연출만 뒤에 붙인다 |
| 씬 | **양쪽 파티 동시 배치**가 새로 필요하다. 지금 씬은 "아군 5 + 적 N" 구조라, 적 자리에 상대 용병 5명을 세우는 배치 하나만 추가하면 된다 |
| 신규 에셋 | **없음.** 콜로세움 배경(PN-ARENA 를 배경으로 확대 사용 또는 BG-03), 단장·용병 스프라이트 전부 있다 |
| 새 UI | 점수 카운트업 칩 하나 |

### 3-4. 스킵

던전과 같다. 5번을 연속으로 도는 콘텐츠라 **스킵이 더 중요하다**.
결과만 보기를 켜면 지금의 `승리! +30` 배너만 뜬다.

---

## 4. 만드는 순서

1. **던전 먼저.** 아군만 나오면 되고 적은 1마리라 씬 변경이 거의 없다.
   여기서 "판정 먼저 계산 → 연출은 결과를 재생" 이라는 틀을 잡는다.
2. **스킵 토글** — 던전에 붙이고 설정에 저장한다. 아레나가 그대로 쓴다.
3. **아레나.** 양쪽 파티 배치가 새 작업이다. 1번의 틀을 그대로 쓴다.
4. 다듬기 — 점수 카운트업, 층 간판 뒤집기 같은 잔 연출.

---

## 5. 결정해 줘야 하는 것

**5-1. 아레나에 실제 전투를 넣을까, 연출만 넣을까.**
지금 판정은 승률 주사위 한 번이다. 화면에서 두 파티가 싸우는데 결과가 미리
정해져 있으면 "때리는 시늉" 이 된다. 대안은 던전처럼 **전투력 비교**로 바꾸고
실제 전투를 돌리는 것인데, 그러면 나보다 센 상대에게 절대 못 이겨서 랭킹이
고인다. 지금 구조(주사위)가 경쟁 게임으로는 맞다고 본다 — **연출만** 권한다.

**5-2. 던전 실패 연출을 넣을까.**
실패해도 열쇠는 소모된다. 연출까지 보여 주면 손해감이 커진다는 반론이 가능하다.
나는 넣는 쪽을 권한다 — 왜 실패했는지(전투력 부족)가 그림으로 남아야
다음 행동이 생긴다. 다만 실패 연출은 **1초 이내**로 짧게.

**5-3. 던전 수문장을 층마다 바꿀까.**
지금 안은 던전마다 고정이다. 10층 단위로 바꾸면 "새 얼굴이 나왔다" 가 생기는데,
보스 스프라이트가 6장뿐이라 금방 돈다. 전용 에셋을 더 그릴지 정해야 한다.
