# 미구현 정리 — 2026-08-24

자는 동안 정리한 목록이다. **지금 안 되는 것**과 **정해 줘야 하는 것**만 적었다.
끝난 작업 기록은 `작업정리.md` 와 git log 에 있다.

---

## 1. 지금 막아 놓은 것 (서버가 있어야 열린다)

**서버 함수도 화면 배선도 끝났다 (2026-08-25).** `server/server.js` 에 랭킹·프로필·
아레나 표본·친구·연합·연합 보스·VXShop 결제가 들어 있고, 화면은 `game/src/net/live.js`
를 통해 그 값을 읽는다.

**남은 것은 접속 한 줄뿐이다.** `core/cloudsave.js` 와 `net/live.js` 는 둘 다
`window.__V8_SERVER` 를 주워 쓰는데, 이걸 채우는 코드가 없다 — Verse8 의
**비-React 접속법이 문서에 없어서** 추측으로 안 썼다. 문서에 있는 것은
`const { server } = useGameServer()` (React 훅) 뿐이고 이 게임은 vanilla JS 다.
실배포 때 호스트가 주는 방식을 확인하고 거기에 꽂으면 화면이 통째로 살아난다.
붙기 전에 확인하려면 `__dbg.live.initLive(가짜서버)` 로 갈아 끼울 수 있다.

`net/live.js` 의 계약 하나만 기억하면 된다 — **붙든 안 붙든 화면 코드는 한 벌이다.**
화면은 캐시로 즉시 그리고(없으면 데모), 응답이 오면 다시 그린다. 서버가 없으면
모든 pull 이 no-op 이라 로컬 dev 는 예전과 똑같이 돈다.

문서에서 확인해 바로잡은 것: **컬렉션 아이템 식별자는 `it.id` 가 아니라 `it.__id`** 다.
예전 코드는 `deleteCollectionItem('rankings', it.id)` 로 undefined 를 넘겨 삭제가
조용히 실패했다 — best-only 랭킹이 계정마다 낡은 기록을 쌓고 있었을 것이다.

배포(`npx -y @agent8/deploy`)는 **직접 하기로 했다** — 여기서는 시도하지 않는다.

아직 서버로 안 넘긴 것:
- **친구 선물** — 지금도 로컬이다. 선물은 내 골드만 늘리는 일방 행위라 상대 계정을
  안 만진다. "상대가 보낸 것을 내가 받는다" 로 바꾸려면 우편(mail)으로 가야 하고,
  그건 지금 구조로는 안 된다
- **스테이지·아레나 랭킹 보드** — `rankings` 컬렉션은 CP 하나뿐이다. 보드마다
  컬렉션을 파면 유일한 전역 공유 자원을 세 배로 때린다. 시즌 컬렉션
  (`rankings_s1`…)을 설계할 때 같이 정하는 게 맞다
- **연합 보스 전투 연출** — 딜은 파티 CP x 1.723 로 계산해 한 번 올린다
  (sim/alliance-boss.js 기준). 던전처럼 실제 타임드 전투로 바꾸는 것은 별도 작업이다

| 기능 | 지금 상태 | 서버 붙으면 |
|---|---|---|
| 연합 명단·가입 | 데모 8인 고정 명단, 가입 기록은 내 기기에만 | `allianceList/Create/Join/Leave` — 정원은 행 개수로 센다 |
| 연합 보스 | 버튼 누르면 "서버 연동 후" 안내 | `allianceBoss/BossHit/BossLog` — 공유 HP·주 3회·딜 순위 |
| 연합 레벨 | 내 기부만 XP 로 쌓임 (하루 최대 95) | `allianceDonate(단계번호)` — 비용은 서버 표에서 읽는다 |
| 친구 | 데모 5명 + 신청 후보 시드 생성 | `findByNickname/friendRequest/Respond/List/Remove` |
| 아레나 | 날짜 시드로 만든 가짜 상대. **전투 연출은 붙었다**(hp_drain) | 실제 유저 편성 매칭 |
| 랭킹 | 내 점수 주변으로 더미 20명 | `getTopRankings(20)` + `getMyBestRank()` |
| 실결제 | 다이아 상품 누르면 안내 토스트 | `$onItemPurchased` — 멱등(purchaseId 기록), 상품표는 서버에 |

**클라우드 세이브만 실제로 붙어 있다** (`game/src/core/cloudsave.js`, `server/server.js`).
통짜 저장 1단계다 — 판정을 서버로 옮기는 2단계는 `save-schema.json > cloudSave.phase2` 에
설계만 적혀 있다.

---

## 2. 물어볼 것 (내가 정하면 안 되는 것)

### 2-1. 패시브 판정 — 결정됨 (C, 전 종류)

2026-08-25 확정. **패시브 16종 전부 실제 판정을 갖는다.** 합산은
`game/src/core/passives.js` 한 곳에서만 한다 — 전투(scene.js)와 파티 DPS(main.js)가
같은 값을 읽는다.

배율 규칙은 툴팁(`skillDesc`)과 글자 그대로 같다:
`pct / add / atkPct / atkRatio / maxHpRatioPerSec` 는 등급·레벨로 자라고,
`chance / hpThreshold / pctPerStack / maxStacks / mult / sec / pctPerSec / maxPct` 는
데이터 값 그대로다. 확률이 레벨로 자라면 상한 100% 에 금방 닿아 등급 사다리가 무의미해진다.

전투 모델에 없는 스탯은 같은 뜻으로 환산했다 (내가 정한 것 — 뒤집을 수 있다):
- **방어력·체력 강화** → 받는 피해 `1/(1+def+hp)`. 파티 최대 체력은 요구 CP 로
  정해진 난이도 기준선이라 늘릴 수 없다
- **관통력** → 피해 증가. 스테이지는 DPS 체크라 적 DEF 가 없다
- **치명타율·치명타 피해** → 바탕 15% / x2 에 가산. 예전엔 `Math.random() < 0.15` 고정이라
  "치명타 확률 +9%" 툴팁이 전투와 아무 관계가 없었다

시뮬레이터(`sim/engine.js`)도 같은 16종을 굴리도록 맞췄고 `sim/skill-coef.json` 을
다시 쟀다. CP-실전 상관은 0.8243 → **0.8828** (목표 0.9 는 아직 미달, 원래도 미달이었다).

**남은 구멍 둘** (데이터 설계 문제라 손 안 댔다):
- `SK-P12 치명타 피해` 는 치명타율이 0 인 유닛에게 값이 0 이다. 전사·마법사는
  직군 치명타율이 0 이라 `SK-P04` 와 같이 껴야만 의미가 생긴다
- 시뮬은 `SK-P11 관통력` 을 평균의 5.5배로 재는데(감산을 통째로 깎으니까) 실제 게임은
  +6% 안팎이다. 스테이지에 DEF 가 없어서 생기는 시뮬-게임 괴리다

### 2-1-옛. 패시브 스킬 20종에 전투 연출을 붙일까 (기록)

지금 패시브는 **전투력에만 곱해진다**. 흡혈·반사·처형·부활 같은 것이 실제로
발동하는 순간이 전투 판정에 없어서, 붙이려면 판정부터 만들어야 한다.

- **A. 그대로 둔다** — 방치 게임에서 패시브는 원래 숫자다. 작업 없음.
- **B. 연출만 붙인다** — 확률적으로 반짝이게. 실제 효과는 여전히 CP 뿐이라
  "속이는" 연출이 된다.
- **C. 판정까지 만든다** — 흡혈은 파티 HP 회복, 반사는 적에게 되돌림, 부활은
  전멸 시 1회 부활. 전투 밸런스를 다시 재야 한다.

내 추천은 **C 를 부활·흡혈 둘만** 이다. 그 둘은 보스전에서 체감이 크고,
나머지(크리·관통 등)는 이미 CP 로 반영돼 중복이다.

### 2-2. 연합 정원 — 결정됨

30명 그대로 간다 (2026-08-24 확정).

### 2-3. 잡몹 구간의 생존 스킬

보호막·회복은 지금 **보스전에서만** 의미가 있다. 잡몹 피해 배수가 0.05 라
스테이지 진행 중에는 죽지 않기 때문이다. 잡몹 피해를 올리면 생존 스킬이
살아나지만, "켜두면 알아서 올라간다" 는 방치 게임의 전제가 흔들린다.
지금은 **안 건드렸다**.

---

## 3. 에셋 대기 (프롬프트는 다 써 뒀다)

`에셋_생성_프롬프트.md` 참고.

| 절 | 에셋 | 쓰이는 곳 | 지금 |
|---|---|---|---|
| 15-1 | `SHOP-D1`~`D6` | 상점 다이아 탭 6상품 | 글자·숫자만 있는 목록 |
| 15-2 | `SHOP-SPEED` | 3배속 해금 칸 | 배경 없음 |
| 15-3 | `IC-LOCK-S` | 작은 잠금 배지 | 큰 자물쇠(UI-LOCK)를 11px 로 줄여 씀 |
| 13 | `PN-ALLIANCE` 등 | 연합·전직·장비 패널 배너 | 다른 그림을 빌려 씀 (AL-BG / PR / ALT-03) |

던전 그림(`DG-05`)도 한 장 비어 있다 — 열쇠 아이콘으로 대체 중.

---

## 4. 배포 전 남은 것

- **WebP 변환 + 아틀라스 패킹** — 에셋 36MB. 배포 직전에 한 번에 한다
  (지금 하면 작업 중 파일이 계속 바뀌어 두 번 일한다)
- **사운드** — 전혀 없다. 타격·소환·레벨업·버튼 정도만 있어도 체감이 크다
- **Verse8 배포 설정** — `server/server.js` 는 있지만 실배포는 안 해 봤다

---

## 5. 오늘 고친 것 중 다시 볼 만한 것

- 9-slice 프레임 그림의 남색 속판을 갈색으로 재색했다. **원본은 git 에 있다** —
  색이 마음에 안 들면 `git show <이전커밋>:game/public/assets/ui/9s/UI-CARD.png` 로
  되돌릴 수 있다
- 숫자 서체를 Black Han Sans 에서 시스템 굵은 산세리프로 바꿨다. 게임 제목만
  디스플레이체로 남겼다 — 다른 곳도 디스플레이체가 낫다면 말해 달라
- 특가 탭을 통째로 뺐다. 결제 연동 때 다시 설계하는 편이 낫다고 판단했다

---

## 6. 2026-08-24 추가분

**던전·아레나 전투 연출** — 붙였다. `연출_기획_던전_아레나.md` 6절에 결과가 있다.
던전은 실제 타임드 보스전이 됐고(전투력 비교 판정은 없어졌다), 아레나는 판정을
그대로 둔 채 hp_drain 연출을 얹었다. 전투 중 [포기] 버튼(1탭 즉시)과 던전 목록의 [소탕](깬 최고층 보상만 즉시, 열쇠 1개)이 있다. 결과만 보기 토글은 폐기.

**타이틀 화면** — 레이어 5종(`TT-*`) 적용. 에셋 문서 21절.

**남은 것**
- 아레나 상대는 여전히 날짜 시드 더미다. 서버가 붙으면 `arenaFoes()` 자리에
  `findProfiles()` 결과가 들어간다 — 필드 모양은 이미 `publicProfile()` 과 같다

**던전 수문장 6종(`DGB-*`)** — 생성·적용 완료 (2026-08-24). WebP 로 굽고
trim 실측까지 넣었다. 에셋 문서 22절 참고.

**에셋 용량** — `scene.loadSprite()` 가 `.webp` 를 먼저 찾으므로 남은 PNG 도
한 장씩 구워 넣으면 된다 (코드 수정 불필요). 굽는 방법은 에셋 문서 21절 하단:
브라우저 canvas 로 인코딩하고 `tools/webp-sink.mjs` 가 파일로 받는다.

**Firestore 문서 한도 대응 (2026-08-24)** — `server/server.js` 검증에 leaf 수
상한을 더했다. 1MB(바이트)와 40,000(인덱스 엔트리)은 **별개 축**이라 크기가 작아도
필드 수로 먼저 죽는다. leaf 15,000(≈30,000 엔트리)에서 막는다.
클라 쪽은 `pruneMail()` — 수령한 우편을 20통까지만 남긴다. 세이브에서 **개수로
자라던 유일한 배열**이었다.

아직 볼 것: `own.mercenary` / `own.skill` / `pend.*` / `equipmentInventory` 는
지금은 작지만 상한이 없다. 계정이 오래되면 leaf 가 여기서 자란다 — 다건이 커지면
단일 문서가 아니라 `$collection`(item 당 문서)으로 빼야 한다.

---

## 7. 2026-08-25 작업

**숫자 단위** — 앞 단위가 10만에 닿을 때 넘어간다 (`99999` → `100k` … `99999k` → `100m`).
1e3 마다 접으면 8,317 이 "8.32k" 가 되어 초반 숫자가 전부 뭉개졌다.
바뀐 곳은 `game/src/core/fmt.js` 한 곳뿐이라 전투력·재화·보상이 같이 따라간다.
세 자리 구간은 **내림**이다 — 반올림하면 999,999 가 "1000k" 로 나온다.

**패시브 16종 실판정** — 2절 2-1 참조.

**서버 함수 + 화면 배선** — 1절 참조. 연합 목록·단원·레벨·보스, 친구 목록·신청·수락,
아레나 상대, 전투력 랭킹이 전부 서버 값을 읽는다. 새 파일 `game/src/net/live.js` 가
캐시-후-재렌더 규칙 한 곳을 쥔다. 남은 것은 `window.__V8_SERVER` 를 채우는 접속 한 줄.

## 8. 2026-08-25 추가분 (2차)

**부팅 예열** — 서버가 붙어 있으면 로딩 막대에서 연동을 전부 끝낸다
(`live.warmup()` — 프로필 제출 → 14종 동시 pull → 연합 소속 정합 → 채팅바).
화면을 열 때 데모가 진짜 값으로 갈리는 깜빡임이 없어졌다. 연합 소속은 서버가
진실이다 — 다른 기기에서 탈퇴/해산해도 부팅 때 로컬이 맞춰진다.

**친구 선물 실구현** — 예전에는 혼자 도는 고리였다("선물"은 내 기기 표시,
"받기"는 무조건 내 골드). 이제 `gifts` 컬렉션으로 계정을 넘는다: 보내면 상대
선물함에 행이 생기고, 받기는 **온 것만** 눌린다. 금액은 여전히 받는 쪽 진행도
기준(idleGold). 이틀 지난 선물은 서버가 읽으면서 지운다.

**채팅** — [전체] [연합] 두 탭. 하단 채팅바가 입구다(누르면 열림, 마지막 대화가
흐른다). 새 글은 `subscribeGlobalCollection` 구독으로 즉시 온다 — 연합 방은
`chatAlly_<연합id>` 로 컬렉션을 갈라 남의 연합 대화가 안 내려온다. 도배는
서버가 초당 1줄로 막고, 방마다 최근 120줄만 남긴다. XSS 는 esc() 로 막는다.

**서버 함수 점검** — `server/test-server.mjs` (헤드리스, $global 메모리 흉내).
67 시나리오 전부 통과. 잡은 버그 1: **죽은 보스 재타격** — killed 판정이
hp<=0 이라 시체를 때릴 때마다 단계가 공짜로 올랐다 (tier 1→5 재현).
bossMax 0 또는 HP 0 이면 closed 로 거부하게 고쳤다.

## 9. 2026-08-25 추가분 (3차)

**채팅 프로필** — 줄마다 단장 아바타(직군)가 붙고, 이름·아바타를 누르면 프로필
카드가 뜬다 (전투력·스테이지·칭호·착용 용병·친구 신청). 서버 `getProfile(account)`
추가 — findProfiles 는 CP 대역 조회라 특정 계정을 못 집는다. 채팅 줄에 직군을
굳혀 담는다(줄마다 프로필 조회하면 40줄 = 40쿼리).

**퀘스트 사이클 정리** — 해금 기준은 이미 코드와 정합(furnace Q39→칸 Q40,
crystal Q53→Q54, trial Q67→Q68). quests.json 의 낡은 6칸 시절 공식 문자열만
7칸 실구현에 맞춰 정정. `tools/validate.mjs` 에 던전 해금 ↔ 사이클 칸 정합
불변식 추가 — 표가 바뀌면 이제 기계가 잡는다.

**다국어 마무리** — 언어 5종: 한국어·English·日本語·简体中文·繁體中文.
  · en/ja 사전의 **루트에 떨어진 죽은 키 74개**를 ui 로 병합 (t() 가 못 읽던 번역)
  · 오류 표(ALLY/FR/GIFT/CHAT_ERR)가 t() 를 안 지나던 것 수정
  · **DOM 번역기** (core/i18n.js > watchDom) — 한국어면 no-op, 외국어면
    MutationObserver 가 새 텍스트 노드를 원문=키로 치환. innerHTML 재조립
    수백 곳을 전부 `${t()}` 로 감싸지 않아도 화면 전체가 번역된다.
    조합 문자열("일반 2-5" · "1.6시간" · "Q1 무기 제작")만 코드에서 t() 로 감쌌다
  · 번역이 길어 칸을 넘치면 **글자를 자동 축소**한다 (9px 바닥)
  · zh 화면 검증: 남은 한글 = 닉네임(고유명사) 1개
  · 사전 ui 494 · data 69. 번체는 간체에서 자표 변환(다의자 게임 문맥 고정)

**채팅·친구 해금** — 게이트 없이 항상 열어 두기로 확정 (2026-08-25).

## 10. 2026-08-25 추가분 (4차)

**WebP 변환** — 브라우저 canvas 인코더 + `tools/webp-sink.mjs` (문서 21절 방식,
`/list` 엔드포인트 추가). 397장 전량 변환 후 **작아진 것만 채택** — 픽셀아트
png 는 원본이 이미 작아 258장은 webp 가 오히려 컸다(재인코딩 손해). 결과:
  · 직접 참조(패널·배경·로고 24장): 코드 참조를 .webp 로 치환하고 png 삭제
  · 전투 스프라이트(27장): loadSprite 가 webp 우선이라 png 만 삭제
  · 나머지: png 유지, webp 폐기
  · `scene.setBackground` 가 png 고정이던 것을 loadSprite(webp 우선)로 교체
  배포 39.2 → 38.7MB. 절감이 작은 이유: 큰 파일(fx 시트류)이 전부 png 승.
  **다음 절감은 아틀라스 패킹 몫이다** — webp 로는 더 안 나온다.

**제작대 확률표** — 밴드 화살표 탐색(60번 클릭)을 버리고 **현재 Lv | 다음 Lv
두 열 비교**로 (버섯커 램프 표 문법). 전 등급을 0%까지 그리고 미해금은 자물쇠,
다음 레벨에서 오르는 등급은 초록 강조. 다음 레벨에 새 등급이 열리면 그 줄 아래
해금 안내가 붙는다.

**모래시계 다이아 구매** — 상점 교환 탭에 12개=100 · 40개=300 · 150개=1000
(개당 8.3→6.7 할인 사다리, shop.json > quickHourglass). 제작대 헤더의 모래시계
옆 **+ 버튼**이 상점 교환 탭을 열고 모래시계 칸까지 스크롤한다. 주 수급처는
여전히 던전·연합 상점 — 이건 편의 창구다.

**Verse8 접속 확답** — window.__V8_SERVER 주입은 배포 후 실환경에서 확인.
window 는 모든 모바일 브라우저·웹뷰에 있는 표준 전역이라 기기 문제 없음.
