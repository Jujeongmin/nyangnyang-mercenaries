# 단장 스프라이트 시트 — 생성 프롬프트

이 문서를 **통째로 ChatGPT 에 주고 원화를 첨부하면** 시트가 나온다.
받은 그림은 그대로 못 넣는다 — 배경 제거·라벨 절단·프레임 정렬이 필요하고
그건 스크립트가 한다 (아래 "받은 뒤" 참고).

---

## 범위 — 9종

전투 단장 모습은 **직군 3 × 전직 단계 3 = 9종**이다.

| 단계 | 이름 | 조건 | 스탯 배수 |
|---|---|---|---|
| 1 | 기본 | 훈련소 Lv 0 | 1.00 |
| 2 | 정예 | 훈련소 Lv 40 | 1.25 |
| 3 | 정예 지휘관 | 훈련소 Lv 110 | 1.55 |

| 직군 | 이름 | 1단계 | 2단계 | 3단계 |
|---|---|---|---|---|
| warrior | 검사 단장 | ✅ 완료 | 필요 | 필요 |
| archer | 사수 단장 | ✅ 완료 | 필요 | 필요 |
| mage | 술사 단장 | ✅ 완료 | 필요 | 필요 |

각 종마다 **걷기 8프레임 + 공격 4~8프레임**. 1단계 3종은 끝났고 **6종이 남았다.**

> **주의**: 지금 코드는 단계를 안 본다 (`captain_${cls}` 만 쓴다).
> 단계별 외형을 켜려면 `scene.js` 에 폴백 체인을 넣어야 한다 —
> `captain_{cls}_t{tier}` → 없으면 `captain_{cls}`. 그 작업은 별도다.

---

## 첨부할 것

**해당 종의 원화 PNG 한 장.** webp 말고 png 다 (알파 무손실, 업로드 호환).

| 무엇을 만들 때 | 첨부할 파일 |
|---|---|
| 1단계 (기본) | `assets/captain/captain_<cls>.png` |
| 2단계 (정예) | `assets/captain/PR-<cls>-2.png` |
| 3단계 (정예 지휘관) | `assets/captain/PR-<cls>-3.png` |

**글로 쓴 묘사보다 첨부 그림이 훨씬 강하게 작용한다.** 첨부 없이 텍스트만
주면 딴 캐릭터가 나온다 (실제로 그랬다). 2·3단계는 장비가 달라지므로
**그 단계 원화를 첨부해야 한다** — 1단계 그림으로 만들면 승급이 안 읽힌다.

---

## 공통 규칙 (모든 프롬프트에 그대로 붙인다)

```
CRITICAL RULES — follow exactly

1. IDENTICAL SIZE in every frame. Do NOT scale the character up or down
   between frames. The head must be the same size in all frames, and the
   distance from the top of the head to the feet must be the same.
2. All frames stand on the SAME horizontal ground line.
3. SAME character in every frame — identical colors, gear, proportions,
   and face as the attached reference image. Copy the reference exactly;
   do not redesign the armor, weapon, or colors.
4. FULL body in every frame. Leave generous empty margin around each
   character so nothing is cropped at the cell edge or the canvas edge.
5. PLAIN WHITE background only. No ground, no platform, no pedestal,
   no rocks, no campfire, no leaves, no shadow, no scenery, no motion
   lines, no effects behind the character.
6. Side view, facing RIGHT.
7. Put the frame label ABOVE each frame in small plain dark text.
8. Keep the character in place — no dashing, no leaping, no jumping,
   no moving toward the camera.
```

**1번이 제일 중요하다.** 프레임마다 키가 다르면 재생할 때 캐릭터가 커졌다
작아진다. 정렬 스크립트는 **평행이동만** 하고 크기는 안 건드린다
(`tools/fosheet.py` 와 같은 규칙) — 스케일이 틀리면 나중에 못 고친다.

**4번이 두 번째로 중요하다.** 잘린 픽셀은 되살릴 수 없다. 이전 시도에서
캐릭터 위아래가 판 밖으로 날아가 시트를 통째로 버렸다.

---

## 걷기 — 8프레임

```
A walk-cycle sprite sheet of ONE character, 8 frames in a 2x4 grid,
based on the attached reference image.

CHARACTER: [아래 직군별 묘사]

FRAMES (label each above its cell):
1. CONTACT   2. DOWN   3. PASS   4. UP
5. CONTACT   6. DOWN   7. PASS   8. UP

[공통 규칙 붙이기]
```

## 공격 — 4프레임

```
An attack sprite sheet of ONE character, 4 frames in a 1x4 row,
based on the attached reference image.

CHARACTER: [아래 직군별 묘사]

FRAMES (label each above its cell):
[아래 직군별 동작]

[공통 규칙 붙이기]
```

**공격이 4프레임인 이유**: 처음에 9프레임(IDLE·WINDUP·DASH·LEAP·SLASH1~3·
IMPACT·RECOVERY)을 받았는데 DASH·LEAP·IMPACT·RECOVERY 는 제자리 공격에 안 맞아
버렸다. 8프레임(2×4)도 받아서 쓰고 있으니 4든 8이든 상관없다 — **다만 프레임
수를 `scene.js` 의 `ATTACK_FRAMES` 에 정확히 적어야 한다.**

---

## 직군별 묘사

2·3단계는 **원화를 첨부하고** 아래 문장에서 장비 부분만 그 그림에 맞게 고친다.
얼굴·비율·화풍은 세 단계가 같으므로 그대로 둔다.

### warrior (검사 단장)

```
CHARACTER: chibi orange tabby kitten warrior. Red neck scarf. Brown leather
armor with a red flame emblem. A round wooden shield with a flame emblem in
one hand, a sword with a flaming blade in the other. White paws, white chest
fur, orange striped tail. Big head, small body (about 2.5 heads tall).
Thick clean outlines, flat cel shading, bright saturated colors.
```

```
1. IDLE (sword lowered)          2. WINDUP (sword raised back)
3. SLASH 1 (swinging down-forward)  4. SLASH 2 (follow-through)
```

### archer (사수 단장)

```
CHARACTER: chibi orange tabby kitten archer. Red neck scarf. Brown leather
armor with a red flame emblem on the chest. A wooden recurve bow with gold
ornaments held in one hand. A quiver of arrows on the back. White paws,
white chest fur, orange striped tail. Big head, small body (about 2.5 heads
tall). Thick clean outlines, flat cel shading, bright saturated colors.
```

```
1. IDLE (bow lowered)            2. DRAW (pulling the string back)
3. RELEASE (arrow leaving the bow)  4. FOLLOW-THROUGH
```

### mage (술사 단장)

```
CHARACTER: chibi orange tabby kitten mage. Red neck scarf. Tan and brown robe
with a red flame emblem. A gnarled wooden staff with a red gem at the top,
held in one hand. A small flame floating above the other open palm. White
paws, white chest fur, orange striped tail. Big head, small body (about 2.5
heads tall). Thick clean outlines, flat cel shading, bright saturated colors.
```

```
1. IDLE (staff at rest)          2. CHARGE (raising the staff, gem glowing)
3. CAST (thrusting the staff forward, flame bursting)  4. RECOVER
```

---

## 받은 뒤 (스크립트가 한다)

`Downloads` 에 받아 두면 된다. 처리 순서:

1. **배경 제거** — 모서리에서 flood fill. 캐릭터가 두꺼운 외곽선으로 닫혀
   있어서 가슴털·발의 흰색에는 안 닿는다
2. **라벨 절단** — 제목·프레임 이름 줄을 밴드로 잘라 버린다. 라벨은 무채색
   글자라 유채색 그림과 채도로 갈린다
3. **프레임 검출** — 밴드 안에서 빈 열로 개체를 가른다. 둘이 붙으면 잉크가
   가장 옅은 열에서 자르고, 라벨 조각 같은 잡티는 크기로 거른다
4. **발밑 정렬** — 칸 버퍼에 픽셀을 직접 복사한다. **평행이동만**
5. **균일 격자 시트** — 한 줄 가로 배열로 굽는다
6. **`_idle` 추출** — 공격 시트 1번 프레임을 떼어 대기 자세로 쓴다

산출물:

```
captain_<cls>_walk.png     칸 W×H, N프레임
captain_<cls>_attack.png   칸 W×H, N프레임
captain_<cls>_idle.png     공격 시트 1번 프레임
```

등록해야 할 값 — **눈대중 금지, 스크립트가 뱉는 실측값을 그대로 넣는다**:

| 등록처 | 무엇 |
|---|---|
| `scene.js` → `ATTACK_FRAMES` | 직군별 공격 프레임 수 |
| `scene.js` → `ATTACK_TRIM` | `{ h, footY }` — 칸 안 캐릭터 높이·발 y |
| `scene.js` → `WALK_FRAMES` / `WALK_TRIM` | 걷기 쪽 같은 값 |
| `assets/trim.json` | `captain_<cls>_idle` 항목 |

`ATTACK_TRIM` 을 안 주면 캐릭터가 칸 여백만큼 **작아지고 발이 뜬다**.
`eh` 는 `h × 923/940` 으로 잡는다 — 기존 단장의 `eh/h` 비율이고, 이걸 지켜야
화면 크기가 안 변한다.

**대기 자세를 시트에서 떼는 이유**: 옛 원화와 시트는 다른 모델이 그린 것이라
선 굵기·음영이 다르다. 그대로 두면 공격이 시작되는 순간 질감이 확 바뀐다.

---

## 검수 기준

| 항목 | 합격 | 뜻 |
|---|---|---|
| 발밑 편차 | 0 px | 정렬 후 값. 크면 재생 때 발이 뜬다 |
| 중심 편차 | 0 px | 정렬 후 값 |
| 프레임 간 픽셀 차이 | 20% 이상 | 0% 면 같은 그림이 복제된 것 |
| 캐릭터 높이 편차 | 10% 이내 | 크면 커졌다 작아진다 |
| 잘림 | 없어야 함 | 판 가장자리에 캐릭터가 닿으면 실패 |

앞의 둘은 스크립트가 맞춰 주므로 사실상 **뒤의 셋이 원화 품질 판정**이다.
