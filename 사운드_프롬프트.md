# 효과음 생성 프롬프트 — ElevenLabs Sound Effects

큐 목록의 단일 소스는 `game/public/data/sound.json` 이다. 이 문서는 **그 42종을
어떤 문장으로 만들지**만 담는다.

## 먼저 — 라이선스

무료 등급은 **비상업 전용**이다. 이 게임은 다이아 결제가 붙는 상업 배포물이라
**Starter($6/월, 150생성) 이상**이 필요하다. 유료 등급의 결과물은 로열티 없이
상업 사용 가능하다. 42종 + 재시도 여유가 150 안에 든다.

## 저장 규칙 — 파일명이 곧 큐 id

```
game/public/assets/sfx/sfx_hit_melee.mp3
game/public/assets/sfx/sfx_crit.mp3
```

**큐 id 를 그대로 파일명으로 쓰면 매핑 파일이 아예 필요 없다.** 받은 파일을
그 이름으로 저장해서 폴더에 넣어 주면 된다. 확장자는 mp3.

받은 뒤 내가 96kbps 모노로 다시 굽는다 (`sound.json > format`, 큐당 10~30KB 목표).
원본은 그대로 두고 변환본만 게임에 들어간다.

## 톤 — 실사 폴리. 튀지 않게

두 번 갈아엎은 끝에 정한 방향이다:

1. Kenney 팩 — 8비트·피치카토라 **만화 같았다** (단장 청취 2026-08-25)
2. 판타지 RPG 톤 — 그것도 아니다 (단장 확정 2026-08-26)
3. **지금 방향 — 실제로 녹음한 것 같은, 눈에 안 띄는 소리**

프롬프트에 쓸 말:

- 넣을 것 — `real recording`, `foley`, `natural`, `subtle`, `understated`,
  `close-miked`, `clean`, `dry`, `soft`, `muted`
- **뺄 것** — `fantasy`, `RPG`, `magical`, `arcane`, `orchestral`, `epic`,
  `sparkle`, `shimmer`, `triumphant`, `cartoon`, `8-bit`, `chiptune`
- **잔향(reverb)은 넣지 않는다.** 타격음은 초당 여러 번 나는데 꼬리가 길면
  서로 겹쳐 뭉갠다. `dry, no reverb` 를 UI·타격 계열에 반드시 붙인다.
- **`subtle` · `understated` 를 아끼지 말 것.** 이 게임은 켜 두고 방치하는
  게임이라 소리가 튀면 몇 분 만에 끄게 된다. 존재감보다 **안 거슬리는 것**이 먼저다.

마법 계열(번개·화염·얼음)은 현실에 없는 소리다. **실제 사물의 소리로 대신한다** —
전기 아크, 가스레인지 점화, 얼음 갈라지는 소리. 그게 "현실성 있게" 의 답이다.

## 길이 — 왜 열이 둘인가

ElevenLabs 의 `duration_seconds` 는 **최소 0.5초**다 (0.5~30). 그런데 버튼 탭처럼
0.15초면 되는 소리가 많다. 그래서 두 값을 따로 적었다:

- **duration 설정** — 화면 슬라이더에 넣을 값. 0.5 미만은 못 넣으니 전부 0.5 이상이다
- **실제 소리 길이** — 프롬프트 문장 안에 `about 0.15 seconds` 로 박아 뒀다

0.5초를 받아도 앞쪽 0.15초만 소리고 뒤는 무음이면 된다 — **변환할 때 내가 뒤쪽
무음을 잘라낸다.** 무음은 용량도 거의 안 먹으니 버리는 게 없다.

`prompt_influence` 는 **0.5~0.6 으로 올려라.** 기본값 0.3 은 프롬프트를 느슨하게
따라서 "실사·조용하게" 같은 지시가 자주 무시된다. 너무 높이면(0.9+) 결과가
매번 똑같아져서 재시도가 의미 없어진다.

duration 을 아예 비우면 프롬프트로 알아서 추정하는데, 대개 필요보다 길게 나온다.

---

## P0 — 13종 (여기부터. 없으면 게임이 조용하다)

| 큐 | 파일명 | duration 설정 | 실제 소리 길이 | 프롬프트 (영문 입력) |
|---|---|---|---|---|
| 버튼 탭 | `sfx_tap` | 0.5s | 0.15s | `Soft finger tap on plastic surface, single short click, dry, no reverb, close-miked, subtle, one-shot, about 0.15 seconds` |
| 불가/오류 | `sfx_denied` | 0.5s | 0.35s | `Low muted double thud, blocked mechanical latch that will not move, dry, no reverb, understated, one-shot, about 0.35 seconds` |
| 근접 타격 | `sfx_hit_melee` | 0.5s | 0.25s | `Dull impact on thick leather bag, short blunt thud, dry, no reverb, close-miked, one-shot, about 0.25 seconds` |
| 원거리 타격 | `sfx_hit_range` | 0.5s | 0.25s | `Small dart hitting a wooden board, short sharp knock, dry, no reverb, natural recording, one-shot, about 0.25 seconds` |
| 마법 타격 | `sfx_hit_magic` | 0.5s | 0.3s | `Short electrical arc snap, brief static discharge, dry, no reverb, subtle, one-shot, about 0.3 seconds` |
| 크리티컬 | `sfx_crit` | 0.5s | 0.4s | `Heavy hammer strike on metal anvil, single clear ring, dry, close-miked, natural, one-shot, about 0.4 seconds` |
| 스테이지 승리 | `sfx_stage_win` | 1s | 1s | `Short soft wooden marimba phrase, three ascending notes, calm and understated, dry, one-shot, about 1 second` |
| 소환 발동 | `sfx_summon_cast` | 0.6s | 0.6s | `Air rushing past a microphone, soft short whoosh, natural recording, dry, understated, one-shot, about 0.6 seconds` |
| 드럼롤 | `sfx_drumroll` | 1s | 1s | `Soft brush roll on a snare drum, quiet building tension, close-miked, natural, no reverb, one-shot, about 1 second` |
| 소환 결과 | `sfx_summon_result` | 0.8s | 0.8s | `Wooden box lid opening with a soft latch release, single warm bell tap, natural, dry, one-shot, about 0.8 seconds` |
| 장비 분해 | `sfx_dismantle` | 0.5s | 0.4s | `Small metal parts falling into a tin tray, short scattered clatter, dry, close-miked, one-shot, about 0.4 seconds` |
| 보상 수령 | `sfx_claim` | 0.5s | 0.5s | `Handful of coins dropped into a cloth pouch, short, natural recording, dry, understated, one-shot, about 0.5 seconds` |
| 퀘스트 완료 | `sfx_quest_done` | 0.9s | 0.9s | `Two soft wooden mallet notes rising, quiet and plain, close-miked, dry, no reverb, one-shot, about 0.9 seconds` |

## P1 — 24종

| 큐 | 파일명 | duration 설정 | 실제 소리 길이 | 프롬프트 |
|---|---|---|---|---|
| 팝업 열기 | `sfx_popup_open` | 0.5s | 0.3s | `Drawer sliding open a short distance, soft wooden slide, dry, no reverb, natural, one-shot, about 0.3 seconds` |
| 팝업 닫기 | `sfx_popup_close` | 0.5s | 0.25s | `Drawer sliding shut, soft wooden bump, short, dry, no reverb, natural, one-shot, about 0.25 seconds` |
| 회피 | `sfx_evade` | 0.5s | 0.25s | `Fabric sleeve swishing quickly past microphone, short cloth movement, dry, subtle, one-shot, about 0.25 seconds` |
| 반격 | `sfx_counter` | 0.5s | 0.3s | `Two metal rods knocking together once, short clean clack, dry, close-miked, one-shot, about 0.3 seconds` |
| 피격 | `sfx_damaged` | 0.5s | 0.25s | `Muffled punch into a padded cushion, short soft thump, quiet, dry, no reverb, one-shot, about 0.25 seconds` |
| 유닛 사망 | `sfx_unit_death` | 0.5s | 0.5s | `Heavy sandbag dropping onto wooden floor, dull settling thud, dry, natural recording, one-shot, about 0.5 seconds` |
| 보스 등장 | `sfx_boss_appear` | 1.2s | 1.2s | `Deep low rumble of distant heavy machinery starting, slow build, natural, restrained, one-shot, about 1.2 seconds` |
| 스테이지 패배 | `sfx_stage_lose` | 1s | 1s | `Two low wooden notes descending slowly, quiet and plain, dry, understated, one-shot, about 1 second` |
| 번개 | `sfx_sk_lightning` | 0.5s | 0.5s | `Electrical arc crackle from a high voltage spark gap, short, dry, real recording, one-shot, about 0.5 seconds` |
| 화염 | `sfx_sk_fire` | 0.6s | 0.6s | `Gas burner igniting with a short whoosh of flame, natural recording, dry, one-shot, about 0.6 seconds` |
| 관통 | `sfx_sk_pierce` | 0.5s | 0.35s | `Thin rod whipping through air then striking cardboard, short, dry, natural, one-shot, about 0.35 seconds` |
| 얼음 | `sfx_sk_ice` | 0.5s | 0.5s | `Ice cubes cracking in a glass, short brittle snap, close-miked, natural, dry, one-shot, about 0.5 seconds` |
| 참격 | `sfx_sk_slash` | 0.5s | 0.3s | `Thin blade swishing through air, single short swipe, dry, no reverb, natural, one-shot, about 0.3 seconds` |
| 버프 | `sfx_sk_buff` | 0.6s | 0.6s | `Soft low hum rising briefly then settling, quiet electrical tone, understated, dry, one-shot, about 0.6 seconds` |
| 회복 | `sfx_sk_heal` | 0.6s | 0.6s | `Water droplet falling into a shallow bowl, soft resonant plink, close-miked, natural, one-shot, about 0.6 seconds` |
| 보호막 | `sfx_sk_shield` | 0.5s | 0.5s | `Palm pressing against a pane of glass, soft dull contact with faint hum, dry, subtle, one-shot, about 0.5 seconds` |
| 골드 획득 | `sfx_gold` | 0.5s | 0.35s | `A few metal coins clinking together in a hand, short, close-miked, natural, dry, one-shot, about 0.35 seconds` |
| 다이아 획득 | `sfx_diamond` | 0.5s | 0.4s | `Small glass bead dropped on a stone table, single clear tick, dry, close-miked, one-shot, about 0.4 seconds` |
| 구매 완료 | `sfx_purchase` | 0.6s | 0.6s | `Cash register drawer closing with a soft mechanical click, natural recording, dry, one-shot, about 0.6 seconds` |
| 뱃지 등장 | `sfx_badge` | 0.5s | 0.25s | `Single soft finger tap on a small metal bell, quiet short ding, dry, understated, one-shot, about 0.25 seconds` |
| 타이머 완료 | `sfx_timer_done` | 0.5s | 0.45s | `Kitchen timer finishing with two soft mechanical dings, natural recording, dry, one-shot, about 0.45 seconds` |
| 층 돌파 | `sfx_floor_clear` | 0.6s | 0.6s | `Heavy bolt sliding open on a metal door, short mechanical release, dry, natural, one-shot, about 0.6 seconds` |
| 아레나 승리 | `sfx_arena_win` | 1s | 1s | `Three soft wooden block notes ascending, plain and calm, dry, no reverb, one-shot, about 1 second` |
| 아레나 패배 | `sfx_arena_lose` | 0.9s | 0.9s | `Two muted low drum taps descending, quiet, dry, understated, one-shot, about 0.9 seconds` |

## P2 — 5종 (여유 있을 때)

| 큐 | 파일명 | duration 설정 | 실제 소리 길이 | 프롬프트 |
|---|---|---|---|---|
| 소환수 | `sfx_sk_summon` | 0.6s | 0.6s | `Soft air pop from uncorking a small bottle, short and light, dry, close-miked, one-shot, about 0.6 seconds` |
| 도감 신규 등록 | `sfx_codex_new` | 0.5s | 0.4s | `Single paper page turning in a book, soft short rustle, close-miked, natural, dry, one-shot, about 0.4 seconds` |
| 우편 도착 | `sfx_mail` | 0.5s | 0.35s | `Paper envelope dropped onto a wooden desk, soft light landing, dry, natural, one-shot, about 0.35 seconds` |
| 던전 입장 | `sfx_dungeon_enter` | 0.9s | 0.9s | `Heavy wooden door pushed open slowly, low creak and stop, natural recording, dry, one-shot, about 0.9 seconds` |
| 아레나 매칭 | `sfx_arena_match` | 0.6s | 0.6s | `Two heavy metal weights set down on concrete, short double clunk, dry, natural, one-shot, about 0.6 seconds` |

---

## 통일한 큐 — 따로 안 만든다

**소환 등급 6종(N·R·SR·SSR·UR·LR) → `sfx_summon_result` 하나** (단장 확정 2026-08-26).
등급은 화면이 이미 말하고 있다 — 등급별 폭발 그림(FX-*)과 액자 색이 그 일을 한다.
소리까지 여섯 갈래로 나누면 만들 것만 늘고 구별은 어차피 눈이 한다.

**버튼을 누르는 순간일 뿐인 큐 8종 → `sfx_tap` 하나** (단장 확정 2026-08-26):
`sfx_tab` · `sfx_toggle` · `sfx_equip_swap` · `sfx_levelup` · `sfx_enhance` ·
`sfx_auto_enhance` · `sfx_summon_levelup` · `sfx_grade_unlock`

재생기가 이 id 들을 받으면 `sfx_tap` 을 낸다 — 호출부를 안 고쳐도 되고, 나중에
따로 주고 싶어지면 `sound.json > foldedIntoTap` 에서 다시 갈라내면 된다.

## 변주가 필요한 큐

`sfx_hit_melee` 는 `sound.json` 에서 `variations: 2` 다 — **같은 소리가 초당 여러 번
나면 기계처럼 들려서** 두 종을 번갈아 재생한다. 같은 프롬프트로 두 번 생성해
`sfx_hit_melee.mp3` · `sfx_hit_melee2.mp3` 로 저장하면 재생기가 번갈아 쓴다.

## 안 만들어도 되는 것 — BGM

BGM 4곡은 ElevenLabs 가 아니라 음악 쪽에서 받는다 (`sound.json > sourcing`):
FreePD(퍼블릭 도메인) 또는 Pixabay Music. 둘 다 표기 의무가 없어서 크레딧
화면을 안 만들어도 된다.

ElevenLabs Music 은 **쓰지 않는다.** 셀프서브 요금제의 상업 이용에서 "Studio Games"
가 빠져 있는데, 그 정의가 "수익화되고 **둘 이상의 플랫폼**으로 배포되는 게임" 이다.
지금은 Verse8 한 곳이라 안 걸리지만 스토어에 올리는 순간 위반이 되고, 그때 곡을
다시 구해야 한다. 애초에 루프도 보장 안 된다 — BGM 은 끝과 처음이 붙어야 한다.

## 받은 뒤

`game/public/assets/sfx/` 에 위 이름으로 넣어 주면:
1. 96kbps 모노 mp3 로 변환 (용량·형식 통일)
2. 재생기 `core/sfx.js` 구현 — 동시 8개 · 같은 큐 60ms 간격 · 배치 축약 ·
   첫 탭 언락 · 백그라운드 음소거 (`sound.json > playbackRules`)
3. 게임 이벤트에 연결 (P0 → P1 → P2)

P0 13종만 있어도 붙이기 시작할 수 있다. 나머지는 없으면 그 큐만 조용하고
게임은 정상 동작한다.
