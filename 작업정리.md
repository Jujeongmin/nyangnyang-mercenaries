# 작업 정리 — 2026-08-21 야간

자는 동안 진행한 것과, 깨어나서 정해 줘야 하는 것.

---

## 1. 끝난 것

### 혼 폐기

| | |
|---|---|
| 지운 것 | `merc_soul` · `skill_soul` 재화, `economy.json > soulShop`, 상점 교환 탭의 혼 상점, `CU-08`·`CU-09` |
| 이름만 바꾼 것 | `characters/skills.json > soulValue` → `dupeValue` (등급별 중복 가치 테이블. 캐스케이드 계산에 계속 쓴다) |

**캐스케이드 나머지를 어떻게 할지가 유일한 구멍이었다.** 중복이 레벨로 다 안 들어가면
남는 값을 혼으로 적립하던 자리다. 버리면 N 중복이 영원히 무가치해지고, 새 재화를 만들면
혼이 이름만 바꿔 돌아온다. **트랙별 이월 카운터**에 쌓아 두고 다음 중복 때 합산하게 했다 —
새 재화가 안 생기고 배출 총량도 안 변한다. (`economy.json > cascade.carryNote`)

혼이 없어졌으므로 **UR 획득은 순수 확률**이다. 천장은 넣지 않았다.

### 훈장 → 아레나 화면 안으로

상점 탭이 5개로 줄었다 (소환 · 다이아 · 패스 · 특가 · 교환).
훈장 상점은 아레나 화면 **투기장 훈장** 줄 오른쪽의 22px 아이콘으로 연다.
화면 전환이 아니라 같은 오버레이 안의 전환이라 `‹ 아레나로` 로 돌아온다.

품목 5종 중 2개가 **이미 폐기된 재화**를 팔고 있어서 갈았다.

```
용병 소환권 10      600 훈장 · 일일 5회
스킬 소환권 10      600 훈장 · 일일 5회
장비 소환권 20      300 훈장 · 일일 5회      ← 구 "제련석 대량"
모래시계 5          500 훈장 · 일일 1회      ← 구 "가속권(1일)"
LR 용병 선택권   120,000 훈장 · 시즌 1회
```

모래시계만 **일일 1회**로 묶었다. 제작 타이머를 직접 씹는 유료 재화라 상위 티어라도
하루 5개가 상한이어야 한다.

### 교환 탭 — 파는 게 둘 다 죽은 재화였다

살리라고 해서 열어 봤더니 `골드 → 제련석` / `골드 → 황금 열쇠` 였다. 둘 다 폐기된 재화다.

```
골드 → 장비 소환권   회당 120만 · 일일 20회 · 1.15배씩 악화
골드 → 던전 열쇠     회당  90만 · 일일  2회 · 1.80배씩 악화
```

열쇠는 일일 2회로 세게 묶었다. 일일 지급 3개가 던전의 게이트라 여기서 헐값에 나오면
게이트가 사라진다. **골드를 태우는 창구이지 열쇠 공급처가 아니다.**

### 영혼의 탑 삭제 (던전 5개)

혼이 없어져 "확정 교환 재원"이라는 존재 이유가 사라졌다.
열쇠는 `DK-05` 를 폐기하고 시련의 탑은 `DK-06` 에 그대로 뒀다 — 열쇠 그림이 던전 테마에
맞춰 그려져 있어 번호를 당기면 시련의 탑 열쇠가 영혼의 탑 그림이 된다.

D60 구간(CP 1.03억)에 해금할 게 하나 줄었다. **그 자리는 무한의 탑이 메운다** —
입장 제한이 없어 전투력이 오르는 만큼 계속 층이 열린다.

### 무한의 탑 — 층마다 다른 보상

```
1층 다이아 25 → 2층 스킬 소환권 8 → 3층 장비 소환권 10 → 4층 용병 소환권 8 → 5층 다시 다이아
10층마다  다이아 150 · 장비 소환권 60 · 모래시계 4
```

총량은 고정이고 종류만 돈다. 층이 오를수록 양이 커지면 고층 한 번이 저층 수십 번을 덮어써서
"오늘 몇 층 밀었나"가 무의미해진다.

**용병·스킬 소환권을 실제 재화로 붙였다.** 담을 자리가 없어서 주면 그냥 사라졌다.
지급만 뚫으면 못 쓰는 걸 주는 셈이라 소모까지 같이 뚫었다 — 소환할 때 다이아보다 먼저 쓴다.
10연 할인은 10장을 실제로 다이아로 낼 때만 붙는다.

### 가속권 3종 폐기

`speedup_1h/6h/1d` 가 퀘스트·일일·특가 보상에 남아 있었는데 **받아도 담을 자리가 없어
조용히 사라지고 있었다.** 모래시계로 환산했다. 시간 그대로가 아니라 크게 낮췄다
(1일권 1장 = 모래시계 10개). 시간대로 주면 1일권 3장이 864개가 돼서 무과금이 타이머를
통째로 씹는다.

### 상단바

- `냥이 단장` 고정 문구 → 내 닉네임
- `요구 <전투력>` 제거 (같은 정보가 HUD 진행도에 이미 있다)

### 제작대 탭에도 망치질

지금까지 하단 독에서만 망치가 내려찍혔다. 탭 안의 큰 대장간 그림은 눌러도 아무 반응 없이
결과창만 떠서 고장 난 것처럼 보였다. 같은 0.42초 타격을 붙였다.

> **"그림 안에 있는 망치가 움직이면 좋겠다" 에 대한 답 —** 안 된다.
> 벽에 걸린 망치는 **3단계(FG-03)에만** 있고 1·2단계에는 아예 없다. 1단계는 모루만 있고
> 망치가 없다. 그리고 움직이려면 벽의 원본 자리를 가려야 하는데 벽 무늬가 반복이 아니라
> 통짜로 그려져 있어 자연스럽게 메울 수가 없다.
> 대신 지금 쓰는 `FG-HAMMER.png` 는 **그 FG-03 망치를 오려낸 것**이라 재질은 원화 그대로다.

---

## 2. 에셋 — 203개 중 200개 완료

### 만들어야 하는 것 3장 (전부 이번에 새로 생긴 것)

| ID | 용도 | 지금 상태 |
|---|---|---|
| `CU-11` | 투기장 훈장 | 숫자만 뜬다 |
| `CU-12` | 연합 코인 | ◆ 기호로 대체 중 |
| `IC-SHOP` | 아레나 상점 아이콘 (22px 버튼) | 🛒 이모지로 대체 중 |

프롬프트는 `에셋_생성_프롬프트.md` 에 넣어 뒀다. `IC-SHOP` 은 22px 에서 읽혀야 하므로
형태가 하나여야 한다 — 차양 + 동전 하나까지가 상한이다.

### 전부 코드에 붙였다 (전에는 31장이 파일만 있었다)

**`UI-*` 15장 — 9-slice 프레임.** `tools/nineslice.py` 를 새로 만들었다.
생성기가 전부 1024×1024 정사각으로 뽑아 놔서 그대로 쓰면 투명 여백이 테두리 폭으로
계산돼 실제보다 두껍게 잘린다. 불투명 영역으로 크롭하고 모서리 실루엣을 재서 slice 를
계산한다. 결과는 `assets/ui/9s/*.png` + `assets/ui/9slice.json`.

> 함정 하나 기록 — `border-image` 는 `border-style` 이 없으면 **가운데(fill)만 그려진다.**
> 테두리가 통째로 사라져 평평한 판때기로 보인다. 반드시 `solid` 를 같이 준다.

붙인 곳: 오버레이 카드 · 상점 헤더 · 목록 행 · 카드 · 버튼 3종(주/보조/비활성) ·
인벤토리 칸 · 하단 네비 + 선택 표시 · 게이지 · 재화 알약 · 토스트.

**`FR-*` 6장 — 등급 액자.** N 돌 / R 초록 / SR 파랑 / SSR 보라 / UR 금 / LR 무지개.
도감과 프로필 대표 용병에 붙였다. 전에는 1px 테두리 색으로만 구분해서
스크린샷에서 UR 과 SSR 이 구분이 안 됐다.

**`FRE-*` 3장 — 장비 액자.** T1~3 단순 / T4~7 리벳 / T8~10 화려.
색이 아니라 **장식의 양**이 등급이다 — 아이템 색·오라가 이미 있어서 액자까지 색을 넣으면
신호가 둘로 갈린다.

**`PFRAME-*` 4장 — 프로필 원형 액자.** `profile.json > profileFrame.unlocks[].tier` 가
그대로 1~4 에 대응한다. 프레임 색(`--fr`)은 액자를 물들이지 않고 뒤에서 새어 나오게 했다.

**`UI-LOCK` / `UI-DIV` / `UI-CORNER` 3장 — 통짜.**
자물쇠가 이모지라 안드로이드는 파란 자물쇠, iOS 는 노란 자물쇠가 떠서 같은 게임으로 안 보였다.
구분선은 4:1 로 그려진 장식이라 폭에 맞춰 늘리면 40:1 로 짓눌려 그냥 실선이 된다 —
장식은 원본 비율로 가운데에 두고 폭은 그라디언트 선이 채운다.
귀 장식은 오버레이 카드에만 넣었다. 전투 화면에 두면 캐릭터를 가린다.

**`CH-*` 3장 — 보물상자.** 상자 아이콘이 `CU-02`(폐기된 황금 열쇠)를 쓰고 있었다.
누적 30% / 70% 를 기준으로 3단계로 찬다. 숫자를 읽어야 얼마나 찼는지 아는 구조면
복귀 동기가 안 생긴다.

---

## 3. 검사 결과

```
node tools/validate.mjs   불변식 전부 통과 (22개 파일)
node tools/audit.mjs      죽은 재화 0 · 미결정 0 · 없는 에셋은 제작 대기분뿐
tools/screens.js          26개 화면 x 5해상도 — 겹침 0 · 밖으로 튄 것 0 · 글자 잘림 0
                          (360x640 / 375x667 / 390x844 / 412x915 / 430x932)
```

브라우저에서 돌리는 법:

```js
const s = await import('/tools/screens.js');
console.log(s.report(await s.runAll((w, h) => resizeTo(w, h))));
```

> **처음에 "5해상도 통과" 라고 적었던 건 틀렸다.** `sweep()` 에 크기 변경 함수를
> 안 넘겨서 **같은 크기로 5번 잰 것**이었다. 실제로 360x640 으로 줄여 보니 사이드 열이
> 퀘스트 배너·보물상자·스킬바를 덮고 있었다 (UI 킷 프레임이 버튼을 34px→45px 로 키운 탓).
> `sweep()` 은 이제 resize 없이 부르면 예외를 던진다.

새로 만든 검사기 두 개:

| 도구 | 무엇을 잡나 |
|---|---|
| `tools/audit.mjs` | 보상표에는 있는데 **담을 자리가 없어 조용히 사라지는** 재화. 이번에 62곳 나왔다 |
| `uicheck.js > strict()` | 자식이 부모 밖으로 튀거나 글자가 잘리는 것. 목록에 없는 요소까지 전부 훑는다 |

작업 중 잡은 실제 버그 7개:

1. `pullOne` 에 이벤트 객체가 인자로 새서 제작대 클릭이 죽어 있었다
2. `#fgHero img` 특이도가 `#fgHeroHam` 을 이겨 망치가 337px 로 부풀었다 (독에서 났던 것과 같은 함정)
3. `profile.js` 의 `.pf-titles` · `.pf-mercs` 닫는 태그 누락 → 대표 용병이 칭호 그리드에 끼어 있었다
4. `.pf-m` 의 `overflow:hidden` 이 등급 액자를 잘라 아예 안 보였다
5. 무한의 탑 설명에 `**다른 재화**` 별표가 그대로 노출 → `fmt.js > mdb()` 추가
6. 도감 등급 액자가 `inset:-9px` 라 위아래 칸끼리 붙어 한 칸처럼 보였다 (SSR 2개가 세로로 이어짐)
7. 등급 액자가 용병 이름을 덮어 누가 누군지 못 읽었다

---

## 3-2. 그 뒤에 더 한 것

### 소환 탭 개편

용병 / 스킬 **토글**. 고른 쪽 하나만 크게 박는다 — 둘을 나란히 놓으니 소환진 그림이
둘 다 반쪽이 됐다. 소환은 이 게임의 간판 화면이다.

**소환 레벨 보상**을 붙였다. 레벨이 오르면 소환권을 주는데 자동으로 넣지 않고
**빨간 점 → 눌러서 수령**이다. 자동이면 레벨이 오른 것 자체를 모르고 지나간다.
빨간 점은 하단 네비 상점 아이콘까지 올라온다.

```
Lv 1~10   레벨당 1장      Lv 11~20  2장     Lv 21~30  3장
Lv 31~40  5장             Lv 41~50  8장
```

무과금이 D180 에 실제로 닿는 구간이 Lv25~30 이라 두 트랙 합쳐 약 120뽑,
총 예산의 3% 다. Lv50 은 한 트랙에만 3,840뽑이 필요해 사실상 과금 구간이다.

### 소환 연출 중 상점이 닫히던 버그

`shop.close()` 를 부르고 연출을 띄우고 있었다. `#reveal` 은 z-index 80,
`#shop` 은 70 이라 **이미 덮는다** — 닫으니 페이드되는 동안 뒤에 메인 화면이 비쳤다.
닫지 않게 고쳤다. 겸사겸사 소환 연출 CSS 가 통째로 두 번 들어가 있던 것(44줄)도 지웠다.

### 죽은 재화 62곳 정리

`tools/audit.mjs` 가 찾아냈다. 전부 **받아도 담을 자리가 없어 조용히 사라지고 있었다.**

| 죽은 것 | 어떻게 | 왜 |
|---|---|---|
| 가속권 1h/6h/1d (10곳) | 모래시계로, 시간 그대로가 아니라 크게 낮춰 | 시간대로면 1일권 3장이 864개 — 무과금이 타이머를 통째로 씹는다 |
| 황금 열쇠 (16곳) | 장비 소환권 1:1 | 에셋 표가 이미 "역할이 옮겨갔다"고 적고 있다. 이름만 바뀐 같은 재화 |
| 제련석 (11곳) | 삭제 | 게이트 자체가 없어진 거라 골드로 바꿔 주면 없앤 게이트를 되돌려주는 셈 |
| SSR 선택권 (4곳) | 다이아 2,800 | 아래 결정 참고 |
| 장비 트랙 재화 | `golden_key` → `equip_ticket` | `gacha.json` 이 폐기된 재화를 적고 있었다 |

### 결정된 것 (사용자 확인 완료)

- **확정 획득 경로 전부 삭제.** SSR 선택권 4곳 · 훈장 12만짜리 LR 선택권을 뺐다.
  근거는 `gacha.json > noPity` 에 남겼다 — 확정 경로가 하나라도 남으면 유저는 확률을
  무시하고 그 경로만 본다. **라이브 후 UR 첫 획득 시점 분포를 반드시 봐야 한다.**
- **탑 소환권 배출은 그대로.** 100층은 CP 2.1억이 필요해 무과금이 D180 안에 닿기 어렵다.
- 다음 작업: Verse8 연동 · 용병/스킬 성장 구현 · 연합 설계.

---

## 3-3. 마지막 구간 — 성장 · 연합 · 검사 자동화

### 용병·스킬 성장 (캐스케이드) — **없던 기능이었다**

`cpOf` 가 `m.level` 을 쓰는데 **레벨을 올리는 코드가 어디에도 없었다.**
UR 을 두 번 뽑아도 전투력이 그대로였다. `economy.json > cascade` 를 그대로 구현했다.

```
중복 가치   N 1 · R 3 · SR 10 · SSR 30 · UR 100 · LR 300
레벨 1 비용 = 올릴 대상의 등급 가치   (N 100장 = UR 1레벨)
대상        같은 트랙에서 전투력이 가장 높은, 캡 미달인 것
나머지      트랙별 이월 카운터에 남아 다음 중복 때 합산
```

만들면서 두 가지를 고쳤다.

1. 처음엔 **맨 앞 대상만 보고 못 사면 멈췄다.** SSR(30) 때문에 SR(10) 을 올릴 수 있는데도
   이월만 쌓였다. 살 수 있는 첫 대상을 고르게 바꿨다.
2. 편성에서 밀려나면 **부어 넣은 레벨이 증발**했다. 지금은 `가치 × (1 + 레벨)` 로
   돌려준다 — 안 그러면 "강화했더니 손해"가 된다.

검증: LR 5인 파티에 60뽑 → 이월 237 누적 → 300 돌파 시 숲사슴 궁수 Lv1,
전투력 76.1m → 79.3m.

### 용병 / 스킬 탭 — 미구현이던 두 탭

하단 네비의 두 탭이 "미구현" 토스트만 띄우고 있었다. 레벨이 오르기 시작했는데
**볼 데가 없어서** 만들었다. 편성 목록(등급 액자 · Lv/캡 · 전투력 기여 %) +
**이월 카운터와 다음 레벨까지의 게이지**.

> 앞서 물어봤던 "이월을 어디에 보여줄까"는 이걸로 답이 났다.
> 용병/스킬 탭이 생겼으니 거기가 유일하게 맞는 자리다.

### 연합 설계 — `data/alliance.json` 신규

`data/` 에 JSON 이 아예 없던 자리다. 핵심 결정 하나:

> **연합은 재화를 만드는 곳이 아니라 쓰는 곳이다.**
> 다이아·용병/스킬 소환권을 일절 배출하지 않는다. 하나라도 넣으면 총 뽑기 예산을
> 다시 계산해야 하고, 연합이 "가입 안 하면 손해"가 되어 사실상 강제가 된다.

```
정원 30명 · 창설 5m 골드 · St30 이상
기부   골드 2m → 코인 10 (일일 5)   장비권 20 → 코인 6 (일일 3)
보스   주 3회 · HP = 연합 CP 합 × 0.55 × 단계(1 → 3.2)
       참가만 해도 코인 25 + 골드 3m / 처치 시 연합 전체에 코인 40 + 골드 5m
상점   골드 · 모래시계(주 6개 상한) · 장비 소환권 · 연합 프레임
```

골드 순유입 주 약 +2,400만 = 무과금 생애 골드(150억)의 0.16%/주.
동시에 **환전소에 이어 두 번째 무한 골드 소모처**가 생겼다.

화면도 4탭(연합·보스·상점·단원)으로 붙였다. 실제 가입·딜 집계는 서버가 해야 해서
단원 탭은 아직 설계 노출까지다.

### 검사 자동화 — `tools/screens.js`

화면 26개를 전부 열어 정렬·겹침을 잰다. 새 화면을 만들면 한 줄 추가한다.

```
26개 화면 × 360x640 / 375x667 / 390x844 / 412x915 / 430x932 — 전부 통과
```

---

## 4. 정해 줘야 하는 것

### (1) 연합 보스 계수 검증

`alliance.json > boss.hp.formula` 의 **0.55 는 검증 안 된 값**이다.
sim 으로 "주 3회 시도에 상위 연합이 5일차쯤 잡는다"가 나오는지 돌려 봐야 한다.
너무 낮으면 첫날 끝나고, 너무 높으면 아무도 못 잡는다.

같이 정할 것 — **연합 탈퇴 시 코인 처리.** 지금 설계는 개인 보유라 그대로 들고 나간다.
그러면 코인만 모으고 나가는 게 이득인지 계산이 필요하다.

### (3) Verse8 서버 연동

저장 · 랭킹 제출 · 광고 보상. 지금은 전부 localStorage 다.

**막힌 지점 하나** — `app/src/net/` 에 백엔드 추상화(local / verse8)가 이미 서 있는데
`main.js` 가 그걸 **하나도 안 쓴다.** 자기 `S` 를 들고 직접 localStorage 에 쓴다.
그래서 "Verse8 연동"은 SDK 를 붙이는 일이 아니라 **main.js 를 net 계층 위로 올리는
리팩터링**이다. 그리고 `verse8.js` 는 프로젝트가 주입해 주는 `remoteFunction` 이
있어야 동작하는데, 그건 Verse8 프로젝트 등록 뒤에나 생긴다 — 지금은 검증이 불가능하다.

### (4) 보상형 광고 — `@verse8/ads` (2026-08-21 지시분)

**아직 안 붙였다.** 호출 자리만 `app/src/net/ads.js` 로 만들어 뒀고, SDK 가 없으면
즉시 성공을 돌려준다. 광고를 쓰는 두 곳(방치 2배 수령 · 2시간 즉시 수령)은 이미
그 창구만 부르므로, **SDK 를 꽂을 때 호출부는 안 고쳐도 된다.**

붙일 때 지킬 것:

```jsonc
// package.json — 루트에 아직 없다. 만들 때 이 버전으로 적는다
"dependencies": { "@verse8/ads": "^0.5.0" }
```

```js
import { Verse8Ads } from "@verse8/ads";

// 타임아웃 없음 — 호스트가 응답할 때까지 무한정 대기
const result = await Verse8Ads.showRewarded({ placementId: "rewarded_1" });
```

**timeout 을 넘기지 않는다.** 클라가 임의로 끊으면 유저는 광고를 끝까지 봤는데
보상이 안 나오는 상태가 되고, 그 판정을 클라가 쥐게 되어 조작 창구가 된다.

**막힌 지점** — 이 프로젝트는 루트에 `package.json` 이 없고 번들러도 없다
(README: "서버 없이 정적 파일"). PixiJS 도 CDN `<script>` 태그다. npm 패키지를
`import` 하려면 **정적 로딩을 접고 번들 빌드로 옮겨야 한다** — 실행 방법 · README ·
`tools/screens.js` 경로가 전부 바뀐다. 별도 결정이라 임의로 진행하지 않았다.

---

## 5. 내가 판단한 것 (뒤집을 수 있게 적어 둠)

| 판단 | 이유 |
|---|---|
| 캐스케이드 나머지 = 이월 카운터 | 버리면 N 중복이 무가치, 새 재화면 혼이 이름만 바꿔 돌아옴 |
| 천장 안 넣음 | "혼 없애고" 만 지시받았고, 천장은 별개 결정이라 임의로 넣지 않았다 |
| `DK-06` 번호 안 당김 | 열쇠 그림이 던전 테마에 묶여 있다 |
| 훈장 상점 품목 2개 교체 | 폐기된 재화를 팔고 있었다. 훈장 총 배출량은 그대로 |
| 환전소 품목 2개 교체 | 위와 같은 이유 |
| 가속권 → 모래시계 환산율을 크게 낮춤 | 시간 그대로면 무과금이 제작 타이머를 통째로 씹는다 |
| 등급 액자는 색, 장비 액자는 장식량 | 장비는 이미 아이템 색 + 오라가 있어 색 신호가 셋이 되면 안 읽힌다 |
| 이월 카운터를 용병/스킬 탭에 상시 표시 | 탭을 만들고 나니 거기가 유일하게 맞는 자리였다 |
| 편성에서 빠질 때 레벨을 전액 환급 | 안 그러면 "강화했더니 손해"가 된다 |
| 연합은 뽑기 재화를 일절 배출 안 함 | 배출하면 예산 재계산 + 연합이 사실상 강제가 된다 |
| 연합 채팅 안 넣음 | 메인 하단에 이미 채팅바가 있다. 둘이면 어디를 봐야 할지 헷갈린다 |
| 소환 레벨 보상은 수동 수령 | 자동이면 레벨이 오른 것 자체를 모르고 지나간다 |


---

# 2026-08-23 심야 자율 작업분 (사용자 취침 중)

## 끝낸 것

### 배속 시스템 (지시받은 사양대로)
- **1x 기본 / 2x 퀘스트 Q5 / 3x 상점 일회성 패키지**
- 데이터: `combat.json > clientRendering.speedUp` 이 해금 조건 표가 됐다 (전엔 [1,2,3] 장식).
  `quests.json > speedUnlockQuests` (단일 소스) · `shop.json > packages > speed3_unlock`
- 3배속 패키지 = 영구 해금 + **매일 다이아 100 수동 수령** (자동 지급이면 받은 줄 모른다).
  월 3,000 으로 과금자 월 구매량 132,000 의 2.3% — 격차 계산에 영향 없음.
  구독이 아닌 이유: subscription.philosophy "성능은 팔지 않는다"와 정면 충돌이라 일회성으로 뺐다
- **방치 수익 = 156.5 × 해금 최고 배속 × 0.35** (`stages.json > idleReward.speedBasis`).
  전엔 배속 없이 고정이라 3배속 온라인 파밍이 방치의 8.6배였다 — 격차가 의도한 2.9배로 복귀
- 잠긴 배속 버튼은 숨기지 않고 자물쇠+점선으로 보여준다. 누르면 해금 조건 토스트
- 상점 구매는 dev 빌드만 즉시 해금(테스트용), 프로덕션 번들엔 그 분기 자체가 없다 (빌드 grep 으로 확인)
- 세이브: `speed` / `speed3` / `speed3DailyAt` (save-schema.json 에 서버 권한 명시)

### 출석 (벤치마크 개선안 4장)
- 데이터는 `dailies.json > attendance` 에 **이미 다 있었다** (7일 주기 + 월 누적 28일). UI 만 없었다
- 사이드 [출석] 버튼이 진짜 화면을 연다. 7일 격자 + 수령 버튼 + 월 누적 마일스톤 6칸
- 7일 주기는 끊겨도 진행 유지(데이터의 resetRule 그대로), 월 바뀌면 누적·수령기록 리셋
- 검증: 1일차 수령 다이아 +150, 중복 수령 차단, 누적 5일 보상 +500, 미달 마일스톤 비활성

### 벽 안내 패널 (개선안 5장)
- 보스 실패 1.6초 뒤 요구 CP / 현재 CP / 진행바 / **그 벽의 gates** (`curve.walls`) / 추천 3버튼
  (훈련소·제작대·던전 — 누르면 바로 그 화면)
- 있는 데이터를 화면에 꺼낸 것뿐. 새 수치 없음. 디버그: `window.__wall()`

### 등급 오라 (지시: 링 + UR/LR 파티클)
- 발밑 등급색 이중 타원 링, **SR 부터** (N·R 까지 칠하면 전원이 빛나 아무도 안 빛난다)
- UR·LR 만 궤도 입자 3개 추가. 파티클 시스템 없이 Graphics — 방치형 상시 이펙트 상한 고려
- 색은 도감 GC 팔레트와 동일 (SR 파랑 / SSR 보라 / UR 주황 / LR 핑크)

### 절전 모드 (개선안 2장 skipBattle)
- 배속 옆 💤 버튼 → 렌더 10fps (PixiJS maxFPS 하한) + "절전 중" 덮개. 탭하면 해제
- 시뮬은 deltaMS 기반이라 전투 결과·보상 동일. combat.json > skipBattle 에 구현 기록

### 그 외
- 스킬 아이콘 사각 14종 → 원형 클립 (경계 3px 페더). CSS 마스크는 82%→99% 로 좁힘 (안전망으로 유지)
- R-03 떠 있는 화살 제거 (알파 분리 성분 11,129px). 가로 중심 587→535 복귀
- 에셋 도구 6종 경로 `assets/` → `game/public/assets/` (Vite 이전 잔재)
- 배경 지우개 툴 신규: `/bgedit.html` + `tools/bgedit-plugin.js` (dev 전용, 자동 백업)

전 항목 검증: `screens.js` [] · `npm run check` 통과 · `npm run build` 통과 · 콘솔 에러 0

## 보류 — 사용자 결정 필요

1. **스킬 이름 충돌.** `에셋_생성_프롬프트.md`(STEP 7)와 `skills.json` 의 이름이 7종 다르다
   (SK-A01 문서 "낙뢰" vs 데이터 "참격" 등). 어느 쪽으로 통일?
2. **출석 day3 보상이 빈 객체다** (`dailies.json > attendance.cycle.rewards[2].grant = {}`).
   지금은 "출석 완료" 토스트만 뜬다. 의도인지 실수인지?
3. **캐릭터 렌더 크기 이상치.** R-04(649px)·N-06(658px)이 R-03(961px)보다 1.5배 크게 렌더된다.
   trim bbox 세로가 표준이라 그림 여백이 크기를 정한다. 고치려면 trim.json 에 몸통 bbox 를
   추가해 32종 전체 크기를 재정렬해야 한다 — 화면 전체가 바뀌는 결정이라 보류
4. **연합 보스 HP 0.55 sim 검증 · 탈퇴 시 코인 처리** — 기존 보류 그대로
5. **도감 일러 20장 미생성** (N4·R8·SR8). UR-03-ART 는 팔레트가 캐릭터와 달라 재생성 프롬프트를
   전달했음 (보라/은백 잠금 + 조준경 금지 문구 포함)
6. **f2pDailyYield 재계산.** 방치 수익이 2배속 기준이 되면서 일일 골드 배출이 늘었다.
   goldsinks.json 예산(150억)과 대조 필요 — sim 을 돌려야 확정된다


---

# 2026-08-23 심야 2차분 (사용자 취침 중, 지시 목록 순차 처리)

## 끝낸 것

1. **스킬 이름** — 확인 결과 프롬프트 문서와 skills.json 이 **이미 일치**했다 (32건 diff 0).
   어제 "7종 다르다" 보고는 cp949 콘솔로 깨진 출력을 잘못 읽은 오판이었다
2. **출석 day3** — 골드 60,000. 뽑기 재화가 아니라 배출 상쇄 불변식에 안 걸리고,
   액수는 f2pDailyYield day1 골드(120k)의 절반
3. **캐릭터 렌더 크기 통일** — 세로 bbox 대신 **면적 등가 높이(eh)** 로 스케일
   (tools/trim.py 가 sqrt(불투명면적)을 char 중앙값 기준 높이로 환산해 기록,
   rig 는 tr.eh 우선). 화면 면적 편차 최대 2.2배 → 1% 이내. 적·보스는 안 건드림
4. **연합 보스 sim** (`sim/alliance-boss.js` 신규 · `sim/package.json` 으로 CJS 부활 —
   Vite type:module 이후 sim 전체가 죽어 있었다)
   - 시도딜 = 파티CP × 1.723 (선형, 틱 시뮬 실측)
   - **HP∝합CP 구조는 연합 규모·강함의 진도 차이를 안 만든다. 유일한 차등축 = 참여율**
   - 계수 0.55 → **0.5**: 참여 90% 연합은 t5 를 4~5일차 클리어(60/60), 65% 는 t4 종점(t5 0/60).
     0.55 는 만근해도 t5 26/60 — 협동 보상이 배신당한다. alliance.json 반영, UI 는 데이터 참조로
5. **네비 토글** — 켜진 용병·스킬·던전 탭 재클릭 시 시트 닫힘
6. **절전 오류 수정** — 원인: #pwrSave 가 `#speed .sbtn` 셀렉터에 걸려 클릭 시
   `+undefined=NaN` 이 배속으로 들어가 전투 0배속 정지, 세이브에 null 로 박혀 영구화.
   `[data-sp]` 한정 + 부팅 시 유효값 검증으로 이중 방어
7. **절전 UI 개편** — AOD 풍 (순검정 = OLED 픽셀 꺼짐), 시계, 절전 경과 시간,
   그동안 획득 골드, **밀어서 해제** (82% 이상, 오터치 방지), stage.visible=false 로 GPU 클리어만
8. **유닛 상세 패널** — 용병·스킬·도감 어디서든 보유 칸 탭 → 도감 일러(-ART 32장 전부 적용)
   크게 + 등급색 발광 + 직군·속성 태그 + 실효 스킬 수치(effectScaling 반영)
9. **수동 장착/해제** — 상세 패널 버튼. 빈 칸 우선, 꽉 차면 최저 CP 와 교체(토스트로 알림)
10. **프리셋 3칸** — 시트 상단 [1][2][3]+[현재 편성 저장]. id 만 저장(실객체는 낡는다),
    캐스케이드로 사라진 id 는 건너뛰고 복원
11. **등급 이펙트** — 평타·스킬 타격이 등급 계수를 탄다 (N 0.88 ~ LR 1.42 크기,
    SSR+ 등급색 잔상 1장, UR/LR 등급색 입자). 스킬은 **스킬의 등급** 기준.
    보스 공격은 붉은 대형 타격 + 흔들림 + 입자 — 위협이 화면 언어로 전달된다
12. **연합 마을** (`view/alliance.js` 신규) — 표 4탭에서 걸어 다니는 마을로.
    바닥 탭 = 단장 이동(거리 비례 속도·좌우 반전·걷기 바운스), 건물 4채 = 보스 소굴 /
    기부 창고 / 연합 상점 / 게시판 (건물 앞까지 걸어간 뒤 패널이 열린다),
    데모 주민 3마리 산책. **기부 구현** — 골드 2M→코인10(일5회), 장비소환권 20→코인6(일3회),
    S.allyCoin 로컬 보유 (audit 의 SERVER_ONLY 에서 해제, MAIL_CUR 에 등록)
13. **화면 스킨** — 출석 초록 / 패스 금 / 아레나 적 / 훈련소 청동 / 연합 보라 색 띠 + 제목 발광

검증: 전 화면 screens.js [] · npm run check(불변식·audit 전부 통과) · npm run build 통과

## 보류 — 사용자 결정 필요 (2차)

1. **연합 마을 실시간 주민** — 지금은 데모 봇 3마리다. 진짜 접속 연합원을 띄우려면
   Verse8 presence API (alliance.json > verse8.concurrency) 연동이 필요하다 — 서버 결정 대기
2. **연합 건물 전용 에셋** — 지금 보스소굴=B-01, 창고=CH-01, 상점=IC-SHOP, 게시판=IC-RANK 재활용.
   전용 건물 4장을 뽑을지? (프롬프트 초안은 에셋_생성_프롬프트.md 에 추가 가능)
3. **CU-12(연합 코인)·CU-11·IC-SHOP 이 TODO_ART** — audit 의 미생성 에셋 목록에 있다.
   마을·기부에서 실제로 쓰이기 시작했으니 우선 생성 권장
4. **연합 보스 "때리기" 미구현** — 보스 소굴은 아직 정보 패널이다. 60초 딜 시뮬 전투
   (아레나 엔진 재활용)를 붙일지, 서버 연동까지 기다릴지
5. **프리셋 이름** — 지금 1·2·3 번호뿐. 이름 붙이기(보스용·아레나용 등)를 원하면 말해달라
6. 기존 보류 6건은 작업정리.md 1차분 참조 (f2p 재계산 등)


---

# 2026-08-23 낮 추가분

- **스킬 설명** — skillDesc() 가 effect.kind 에서 문장을 만든다 (26종 전부).
  상세 패널에 표시. 데이터에 문장을 안 넣는 이유: 수치를 고칠 때마다 문장이 낡는다.
  전투 스킬바도 자동 모드에서 탭하면 상세가 뜬다 (수동 모드 탭 = 발동 유지)
- **스킬 쿨타임 UI** — 자동 발동 시 그 칸에 원뿔 와이프가 돌고 끝나면 반짝인다.
  실시간 = cooldownSec / 배속. scene 이 skillCast 이벤트로 알린다
- **연합 이동 = 가상 조이스틱** — 누른 자리에 원 안 원이 뜨고 민 방향·세기로 걷는다.
  데드존 0.12, 짧은 탭(220ms)은 건물 열기. 클릭 목적지 방식 폐기
- **연합 마을 맵** — 중앙 광장 + 흙길 4갈래 + 덤불 경계. 건물 112px(랜드마크 146px)
  + 접지 그림자. 건물 그림은 사용자가 만든 AL-01~04 사용
- **절전 완전판** — veil 을 #app 밖으로 (VS z30·상단바·네비가 덮개 위로 새고 있었다),
  절전 중 #app display:none — CSS 애니·레이아웃·페인트 전부 정지.
  배터리: JS 시뮬 10fps 만 남음 / GPU 클리어만 / OLED 순검정.
  **메모리는 줄지 않는다** — 텍스처·상태를 유지해야 해제가 즉시라서. 의도된 트레이드오프
- **SSR+ 몸 오라** — 공유 글로우 텍스처 1장(등급색 tint·가산·알파 펄스) + 상승 불씨 3개를
  기존 링 Graphics 에 통합. 유닛당 추가 스프라이트 1개가 비용 전부
- 판단: "캐릭터색"은 등급색으로 해석 (발밑 링·잔상과 일관). 속성색을 원하면 말해달라


## 2026-08-23 낮 3차분

- **정리·최적화 (웹게임)** — proto/ 삭제(역사에 남음), PNG 무손실 재압축 386→318MB(-18%,
  픽셀 바이트 검증), dist/ 로컬 정리. **더 줄이려면 손실 압축(1024→512 다운스케일 or WebP)
  결정 필요 — 보류 질문**
- **연합 카메라** — 월드 170%×145%, transform 팔로우 + 경계 클램프. 맵 배경 프롬프트
  STEP 6-A(AL-BG, 2048×1024) 추가 — 코드가 이미 그 경로를 보고 없으면 BG-01 폴백
- **토스트 재설계** — 중앙 상단 큰 토스트 폐기. 획득 = 상단 스택형 [아이콘 이름 +수량]
  배너(합산·자동소멸), 안내 = 중하단 pill. 출석·우편·퀘스트·방치·기부·패스·3배속 전환.
  **장비 착용 CP 토스트 삭제** (상단 CP +n ▲ 배지가 이미 알린다)
- **패스 일괄 수령** — 아무 티어 [받기] = 그 트랙 열린 것 전부. 같은 재화 합산 배너
- **출석 아이콘화** — 보상 글줄 → 재화 아이콘 + 수량
- **연합 패널 실게임 문법** — 홈: 엠블럼+이름+Lv+인원+공지+요약 3칸 / 보스: 일러+HP바+
  내 도전 횟수+[도전](서버 대기 안내) / 단원: 아바타·직위·기여·접속 데모 명단.
  설계 수치 나열(HP 계수·배수 표)은 유저 화면에서 제거

### 보류 추가
- 에셋 다운스케일/WebP 전환 여부 (300MB 는 웹게임 초기 로딩에 무겁다.
  다만 지금은 화면당 필요한 것만 요청되므로 실측 후 결정 권장)
- 연합 보스 데모 HP(72%)·도전 횟수는 로컬 목업 — 서버 컬렉션 연동 시 교체


## 2026-08-23 심야 2일차 (사용자 취침 중)

- **손실 압축 결정·실행** — 256색 양자화(FASTOCTREE+FS 디더). 386→66MB(-83%).
  5장 좌우 비교 시트로 화질 확인 후 전체 적용, trim.json 재생성.
  WebP 를 안 고른 이유: 코드 전반 .png 경로 수정이 필요한데 양자화는 무수정
- **로딩 화면** — 중앙 타이틀+영문 부제 / 하단 진행바, 플레이버 문구 순환.
  첫 실행 언어 선택(ko/en/ja) → S.lang 저장. **실제 번역(i18n)은 보류 결정**
- **AL-BG 적용** — 세로형에 맞춰 월드 150%x175%. 양자화 일괄에 같이 탐(0.6MB)
- **연합 패널 z 버그** — 마을(z70)이 패널(z60)을 가렸다. .over-alli(z82) 부양
- **UI 장르 문법 1차** — 팝업 타이틀 중앙 리본 / 네비 선택 탭 솟음+발밑 발광 /
  재화 캡슐 pill / 다이아 [+] 상점 지름길 / 팝업 등장 애니 / 출석 빨간 점 /
  표면색 하드코딩 58곳 토큰 정규화. 대조표: docs/벤치마크_개선안.md 8장
- shop.open('diamond') 로 탭 직행 지원

### 보류 추가 (아침 결정용)
- **i18n 실작업** — 언어 선택은 저장만 된다. 전 문구 t() 감싸기는 별도 대작업
- **UI 전면 개편의 "전면" 범위** — 장르 문법 추가는 했지만 색·프레임 자체를
  갈아엎는 리스킨인지, 지금 방향(통일성 강화)인지 확인 필요. 후보 목록은 8장 표
- 데미지 숫자 전용 폰트 — Black Han Sans·Jua 가 이미 font/ 에 있다. 전투 숫자에
  어느 쪽을 쓸지 취향 결정만 남음


## 2026-08-24 심야 (사용자 취침 중)

### 치명 버그 2건
- **QUEST_CUR 에 소환권 3종이 없었다.** quests.json 은 Q1 에 merc_ticket 10 을
  주는데 지급 표에 키가 없어 **조용히 증발**했다. Q2(용병 소환 10회)의 재료가
  통째로 사라져 사슬이 첫 칸에서 끊겼다. audit 은 MAIL_CUR 만 봐서 못 잡았다 —
  이제 audit 이 두 표의 어긋남도 검사한다
- **절전 켜면 화면이 아예 안 보였다.** #app 닫는 </div> 가 덮개·토스트 뒤에 있어
  전부 app 안이었고, 절전이 #app 을 display:none 하니 덮개까지 사라졌다.
  구조를 바로잡아 boot/veil/toast/gains 를 body 직속으로

### 밸런스
- Q8 skill_ticket 15→17 (Q9 목표 24, 누적 공급 23 으로 1장 부족했다)
- **validate 에 퀘스트 사슬 불변식 추가** — 목표 재화를 이전 보상으로 못 채우면 실패

### UI 리스킨 (색·프레임 갈아엎기)
- 팔레트를 **차가운 청회색 → 따뜻한 목재/황동**으로. 토큰만 갈아 전 화면 동시 적용
  (--bg #0e131c→#1a1410, --card #141c2a→#33261b 등) + 하드코딩 44곳 후속 치환
- 숫자 폰트를 Black Han Sans 로 확대 적용 (재화·전투력·가격·수량·타이머 등 20여 자리),
  tabular-nums 로 자릿수 흔들림 제거

### 다국어 실작업
- core/i18n.js — **한국어 원문이 곧 키**인 사전 계층. 사전에 없으면 원문 폴백이라
  부분 번역이 안전하다. t()/tn() + /public/i18n/{en,ja}.json (86·83키 씨앗)
- 언어 선택이 로딩 **앞**으로. load() 를 먼저 호출하게 고쳤다 —
  안 그러면 S.lang 이 늘 비어 매번 다시 묻고 부팅이 거기서 멈춘다 (실제로 났다)

### 반응형 — 전 해상도 통과
- **검사기에 줄바꿈 판정 추가** (고아 줄·버튼 3줄 접힘). uicheck.wrap
- @media → **@container** 로 전환. 앱은 480px 로 잘리는데 뷰포트를 재고 있어서
  좁은 화면 규칙이 데스크톱에서 아예 안 걸렸다 (검사가 계속 실패하던 진짜 이유)
- 360×640 에서 23건 → **0건**. 사이드 축소·배속 좌측 이동·HUD 폭 제한·
  퀘스트 배너를 전투 영역 밖으로 이전·스킬칸 폭 식 통일
- 최종: 360/375/390/412/430 **전 해상도·전 화면 통과**

### 연합
- 건물 확대 (112→152px, 랜드마크 146→196px), 중앙 광장에서 시작(y47%),
  이동 속도 30→18, CSS 길·덤불 제거 (AL-BG 배경에 이미 그려져 있어 두 겹이었다)

### 에셋 프롬프트
- STEP 7-B 시즌 패스 프로필 테두리 2종(PF-S1·PF-S1G) 추가.
  **칭호는 글자라 에셋 불필요** — pass.json 이 주는 그림은 프레임 둘뿐이다

### 보류 (아침 확인)
- i18n 씨앗 사전은 UI 고정 문구 위주다. 용병·스킬 이름(64종) 번역은 tn() 배선만
  돼 있고 data 사전이 비어 있다 — 번역 문구를 채울지 결정 필요
- 리스킨은 토큰 기반이라 되돌리기 쉽다. 목재 톤이 아니면 :root 값만 다시 준다
