# 작업 보고 — 2026-08-24

자는 동안 요청한 것들을 하나씩 대응한 기록이다. **결정해 줘야 하는 것**은 맨 아래에 모았다.

---

## 1. 요청 → 대응

| 요청한 것 | 어떻게 했나 |
|---|---|
| 연합 정원 30명 그대로 | 그대로 뒀다. 질문 목록에서 뺐다 |
| 이벤트에 무료 1000뽑 배너가 안 보임 | 배너에 그림 URL 을 **두 장** 넣어서 CSS 레이어 속성이 뒤 그림에만 붙고 앞 그림이 타일로 깔려 있었다. 한 장(EV-02)만 쓰게 고쳤다 — 이제 냥이와 쏟아지는 소환권이 보인다 |
| 뽑 폰트 가독성 | 배너 제목이 Black Han Sans 라 밝은 그림 위에서 글자끼리 붙어 뭉갰다. 굵은 산세리프 + 그림자로 바꿨다 |
| 배너를 그라데이션으로 희미하게 덮지 말 것 | 왼쪽 글자 자리만 좁게 눕히고 오른쪽 58% 부터는 그림을 그대로 보여 준다 |
| 냥냥 주사위에 미션이 어디 있나 | 주사위판·상품·구매 아래 스크롤에 묻혀 있었다. **판 / 미션 두 탭**으로 갈랐고, 주사위가 0개면 미션 탭이 먼저 열린다. 받을 게 있으면 탭에 점이 붙는다 |
| 미션 탭을 스크롤 없이 | 상세 화면의 머리 배너와 축제 히어로(색종이+단장)를 뺐다 — 목록에서 이미 본 그림이 두 겹이었다. 두 탭 다 스크롤 0 |
| 미션 UI 겹침 | "매일 출석" 의 축제 배지가 제목 위로 올라타 있었다. 제목 줄을 줄바꿈 가능하게 하고, 보상 버튼 자리(70px)를 확보했다 |
| 미션 버튼 크기 일정 · 노란 주사위가 안 보임 | 버튼을 58x36 으로 못박고 바탕을 짙은 갈색으로 바꿨다. `.rt-b.go` 가 금색을 덮어써서 특이도를 올려야 했다 |
| 미션에 주사위 이모지 | `EV-DICE-5` 에셋으로 교체 (버튼·구매 칸 전부) |
| 미션이 너무 쉽다 | 소환 5→**50회**, 자동강화 1→**50회**. 주사위 보상도 2→3, 1→3 으로 올렸다 |
| 15/40/100회 보상이 잘림 | 칸을 `minmax(0,1fr)` 로 바꾸고 이름 줄에 최소 높이를 줘 세 줄까지 늘어나게 했다 |
| 주사위 들어가면 상단 배너 안 떠도 됨 | 뺐다 (위 미션 탭 항목과 같은 수정) |
| 랭킹 배너가 잘림 | 노출 지점 58%→40%, 높이 112→128px. 트로피와 고양이 셋이 전부 나온다 |
| 상점 배너에 상인이 잘림 | 노출 지점 56%→34%. 얼굴이 전부 나온다 |
| 배너 크기를 패널에 맞출 것 | 프레임 안쪽(패딩 상자) 기준 절대배치로 바꿔 좌우 오차 0 |
| 닫기 우상단 · 제목 중앙 · 재화 좌상단 | 제목만 절대배치로 못박아 재화 폭과 무관하게 중앙에 온다 |
| 제작대 배경이 겹쳐 보임 | 머리 배너(대장간)와 본문 히어로(FG-01)가 같은 그림 두 장이었다. 히어로를 빼고 배너가 **현재 단계** 그림을 들게 했다 |
| 특가·교환에 갈색 위 갈색 | 9-slice 의 `fill` 을 끄고 CSS 배경을 깔아 두 갈색이 2 씩 어긋나 있었다. `fill` 을 되살려 한 겹으로 두고, 화면 색은 그 위에 덮는 얇은 막으로 준다 |
| 거의 모든 UI가 그렇다 | 옛 청색 스킨의 하드코딩 표면색 43종(CSS 101곳 + JS 6파일)을 갈색으로 변환. 전 화면 자동 점검으로 차가운 표면 0 확인 |
| 인벤토리 장비 카드 색 맞춤 | `--card` 를 프레임 그림의 속색(49,34,22)과 정확히 일치시켰다 |
| 특가는 아예 없애는 게 나을 듯 | 탭 통째로 제거. 6칸 전부 결제 연동 전이었고 절반이 폐기 재화였다 |
| 제련석 없애기 | UI 표기·지급 경로·퀘스트 보상 후보에서 제거 |
| 1x/2x/3x 가 따로 논다 | 낱개 회색 버튼 → **황동 토글 바** 한 덩어리로. 켜진 칸만 금색 |
| 2x/3x 옆 자물쇠 이모지 | 없앴다. 회색으로 죽은 것만으로 읽힌다 |
| 다이아·골드 + 표시 | 초록·금 두 색이던 것을 황동 단추 하나로 통일 |
| 미해금 자물쇠 에셋화 | 이모지 자물쇠 4곳을 `UI-LOCK` 에셋으로 (이모지는 기기마다 모양이 다르다) |
| 미해금 칸 누르면 안내 | 메인 스킬바·용병 탭·스킬 탭 세 곳 모두 "퀘스트 N 개를 더 깨면 열립니다 (현재/목표)" |
| 골드 수급량을 h 말고 액수로 | 보물상자 라벨이 "1.4시간" → **쌓인 골드 액수** |
| 아레나 프로필이 너무 작다 | 34→48px. `#ovb .frow img{15px}` 이 특이도로 이겨 실제로는 15px 로 찌그러져 있었다 |
| 설명 텍스트 정리 | 화면을 보면 아는 것·설계 메모가 새어 나온 것 9곳 제거. 규칙을 알려 주는 문장(가입 조건·확률 고지)은 남겼다 |
| 제작대 자동 중 애니메이션 | 자동이 도는 동안 망치질이 끊기지 않는다. 결과 대기(장비 잡힘)나 정지면 곧바로 선다 |
| 패시브 연출만 붙이기 | 12종에 연출을 붙였다. 타격 순간(치명타·흡혈·반사·관통·이중공격), 처치 순간(즉사·응징·심판), 상시 고리(재생·불굴). **판정은 그대로 전투력에만** 반영된다 |
| CP 표기 | 친구·아레나·던전 모두 "전투력" 으로 |

---

## 2. 에셋 프롬프트 (`에셋_생성_프롬프트.md`)

새로 쓴 것:

- **15절** — 상점 다이아 상품 6종(`SHOP-D1`~`D6`, 값이 오를수록 다이아가 늘고 단장이
  점점 파묻혀 잠), 3배속 카드(`SHOP-SPEED`), 작은 잠금 배지(`IC-LOCK-S`)
- **16절** — 임무 진행 마디(`MQ-NODE` / `MQ-NODE-ON` / `MQ-BAR`), 아레나 훈장
  `CU-11` 교체(지금 128px 로 생성돼 확대하면 뭉갠다), 아레나 프로필 액자(`AR-FRAME`)

이전에 쓴 13절(패널 헤더 PN)·14절(SFX-13~16)은 **다 생성돼서 적용 완료**다.

---

## 3. 정해 줘야 하는 것

### 3-1. 부활 스킬을 무엇으로 바꿀까 ← **가장 중요**

「불굴」(SK-P14, UR)이 지금 부활 스킬이다 — 전투당 1회, 전멸 시 최대 체력 20%로 부활.
지적한 대로 **스테이지에서는 전멸이 안 나서 죽은 스킬**이다.

그런데 파고들어 보니 부활만의 문제가 아니었다. 잡몹 피해 배수가 0.05 라
**생존 계열 전부**가 스테이지에서 무의미하다:

| 스킬 | 지금 효과 | 스테이지에서 |
|---|---|---|
| 불굴 (UR) | 전멸 시 1회 부활 | 전멸 자체가 안 남 |
| 회피 (R) | 피격 7.5% 회피 | 안 죽으니 의미 없음 |
| 반사 (SR) | 받은 피해 14% 반사 | 받는 피해가 미미해 반사도 미미 |
| 재생 (SSR) | 초당 최대체력 1.2% 회복 | 깎이지 않으니 회복할 게 없음 |
| 흡혈 (SR) | 피해의 29.6% 회복 | 같음 (보스전에서만 유효) |

**내 제안** — 다섯 개를 "생존 + 진행" 양쪽에 걸치게 바꾼다:

| 지금 | 바꿀 안 | 효과 |
|---|---|---|
| 불굴 | **투지** | 전투가 길어질수록 피해 증가 (초당 +2%, 최대 +40%). 보스전에서 크고 아레나에서도 유효 |
| 회피 | **선제** | 전투 시작 3초간 공격력 +N%. 웨이브가 짧은 스테이지에서 계속 발동 |
| 반사 | **가시 오라** | 적이 때릴 때마다 **파티 공격력 비례** 고정 반사. 잡몹도 때리므로 스테이지에서 실제 딜 |
| 재생 | **활력** | 초당 회복 + 체력이 가득 차 있으면 공격력 +15% (스테이지에선 사실상 상시) |
| 흡혈 | 그대로 | 보스전 전용임을 설명에 명시 |

이렇게 하면 아레나 전용 스킬이 사라지고, 다섯 개 다 스테이지 진행에 기여한다.
**다만 새 아이콘 5장이 필요하고** 전투 판정도 새로 짜야 한다. 이 방향으로 갈지,
아니면 부활 하나만 다른 걸로 갈아끼울지 정해 달라. 정해 주면 프롬프트도 그때 쓴다.

### 3-2. SSR 이상 강화 재료가 많이 든다 — 지금 설계

강화는 **중복 → 이월 포인트 → 레벨** 구조다. 중복 하나가 주는 값과 레벨 1 올리는
비용이 **둘 다 그 등급의 `dupeValue`** 라, 결과적으로 **자기 등급 중복 1개 = 1레벨**이다.

| 등급 | 중복 1개 값 | 레벨 1 비용 | 만렙 | 만렙까지 필요한 자기 등급 중복 | 하위 등급으로 환산 |
|---|---|---|---|---|---|
| N | 1 | 1 | 10 | 10개 | — |
| R | 3 | 3 | 15 | 15개 | N 45개 |
| SR | 10 | 10 | 20 | 20개 | R 67개 |
| SSR | 30 | 30 | 25 | 25개 | SR 75개 |
| UR | 100 | 100 | 30 | 30개 | SSR 100개 |
| LR | 300 | 300 | 40 | 40개 | UR 120개 |

체감이 튀는 지점은 **SSR 부터 만렙이 25 인데 중복 하나가 30 포인트**라, 하위 등급으로
메우려면 SR 75개가 필요하다는 것이다. N~R 은 중복이 흔해 금방 차는데 SSR 부터
갑자기 벽이 선다.

**손볼 수 있는 곳은 두 군데다.** ① `dupeValue` 의 등급 간 배수(현재 3.3배씩)를
낮추면 하위 중복으로 상위를 올리기 쉬워진다. ② `levelCap` 을 낮추면 만렙까지의
총량이 준다. 어느 쪽을 얼마나 낮출지 말해 주면 그대로 맞춘다 — 밸런스 축이라
내가 임의로 못 정하겠다.

### 3-3. 잡몹 구간 생존 스킬 (3-1 과 연결)

3-1 의 제안대로 가면 이 항목은 자동으로 풀린다. 반대로 "생존 스킬은 보스·아레나
전용이 맞다" 로 가면, 잡몹 피해 배수(0.05)를 올려야 하는데 그건 "켜두면 알아서
올라간다" 는 방치 게임의 전제를 흔든다. 지금은 안 건드렸다.

---

## 4. 아직 서버가 없어서 막혀 있는 것

`미구현_정리.md` 에 따로 정리해 뒀다. 요약하면 연합 명단·보스, 친구 실계정,
아레나 실매칭, 랭킹, 실결제 — 화면은 다 있고 판정만 없다. 클라우드 세이브만
실제로 붙어 있다.
